## 2026-06-28T22:25:05Z — Night Shift run: 12 cycles (PASS 4 / FAIL 8) — stopped by Brian
- Models: cycles on opus; 5 escalated to opus; 1 quality-judged on opus

- PASS — post-photos-survive-auth — logged-out aircraft sellers' uploaded photos now survive the Sign in to Publish redirect and reloads (opt-in localStorage persist/restore gate
- cycle produced no verdict (exit 124)
- PASS — days-on-market-vs-comps — listing pages now show how long a listing has been for sale vs comparable active same-make/model listings (honesty-gated, never fabricated)
- cycle produced no verdict (exit 124)
- PASS — aircraft-model-suggestions — the aircraft Sell your Aircraft form's Model field now offers make-aware curated suggestions (free text still allowed)
- cycle produced no verdict (exit 124)
- cycle produced no verdict (exit 124)
- cycle produced no verdict (exit 124)
- cycle produced no verdict (exit 124)
- PASS — airframe-utilization-read — listing pages now show an honest avg-hours-flown-per-year Airframe time read (TTAF ÷ age) with two-sided buyer guidance
- cycle produced no verdict (exit 124)
- cycle produced no verdict (exit 124)


# Overnight review — 2026-06-28

## 📊 Traffic (PostHog) — as of 2026-06-28

- **Visitors:** 24 all-time · 19 in the last 7 days
- **Pageviews:** 614 all-time · 511 in the last 7 days
- **Not from Oakland:** 22 visitors _(early on, most non-local hits are crawlers/bots, not real users)_

**By city**

| City | Visitors | Pageviews |
|---|--:|--:|
| Oakland | 4 | 577 |
| (unknown) | 4 | 4 |
| Monte Vista | 2 | 2 |
| San Francisco | 1 | 8 |
| Sac City | 1 | 5 |
| Seattle | 1 | 3 |
| El Cerrito | 1 | 2 |
| Fishkill | 1 | 1 |
| Council Bluffs | 1 | 1 |
| Phoenix | 1 | 1 |
| Atlanta | 1 | 1 |
| Denver | 1 | 1 |

**Top pages**

| Page | Pageviews |
|---|--:|
| / | 130 |
| /aircraft | 98 |
| /partnerships | 84 |
| /admin | 54 |
| /partnerships/seeking | 29 |
| /admin/listings | 24 |
| /tools | 12 |
| /admin/backlog | 12 |
| /admin/listings/sample | 11 |
| /admin/review | 9 |

---

**21 cycles landed on staging across 10 pages.** Review the live staging site (you must be logged into Vercel), then tell Claude which pages to promote — or "promote everything."

Staging site: https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app

---

## /aircraft/[make]/[model] — Make + Model "for sale" pages

- **Fixed a silent loss of a saved listing across signup.** When a logged-out visitor hearted a listing, a soft-save prompt offered two paths: "Skip — save on this device" (which stored the listing locally) and the primary "Create a free account" button (which did **not**). So anyone who took the headline CTA — heart a plane → make an account to save it — came back signed in to an **empty heart**: the listing they signed up specifically to save was never persisted, so the device→account merge had nothing to merge. Now the "Create a free account" button writes the listing to the device store the instant it's clicked (synchronously, before the browser leaves for the sign-in page), so the already-existing `DeviceSaveSync` merge reconciles it into the brand-new account on return — the save the user asked for actually lands in `/saved`. Reuses the entire existing soft-save pipeline (`addLocalSave` + `mergeDeviceSaves`): **no schema, no new server action, no new query, no copy/layout change.** 2 files: `src/components/SoftSavePrompt.tsx` (new `onCreateAccount` prop fired on the CTA's `onClick`), `src/components/SaveListingButton.tsx` (passes `onCreateAccount={() => addLocalSave(...)}`). _(cycle: soft-save-keep-intent)_
- **The ClubHanger Estimate panel now shows a small visual price bar that spatially marks where this listing's asking price sits between the cheapest and priciest comparable listing.** Two prior cycles built up the data — `estimate-price-range` added the low–high range, `estimate-spread-position` added the percentile — but both were text-only ("…range $5,000–$435,000 (median $124,500) — this one is priced above 88% of them."). A buyer still had to picture the spread in their head. This cycle renders that as a horizontal **gradient track** (emerald → slate → amber, cheap → expensive) with: a **median tick** (slate vertical line), a **"this listing" marker** (sky-blue dot) positioned by where the asking price falls within the low–high span, `$5,000` / `$435,000` end labels under the track, and a tiny legend ("● this listing | median"). It renders **only in the existing range branch** (`high > low`); the single-value-spread fallback (`high == low`) shows no bar, unchanged. Positions are pure arithmetic over values the panel already computes — `low`, `high`, `median`, and this listing's own asking price recovered as `estimate.median + estimate.deltaDollars` — so **no new data, query, DB read, or claim**, and no new prop on `EstimatePanel`. The subject's own price is excluded from the comp set (so it can fall at/below `low` or at/above `high`), so the marker is **clamped to [0,100]%** — consistent with the existing "priced below/above all of them" copy. A `role="img"` + `aria-label` describes the position for screen readers. 1 file: `src/app/aircraft/listing/[id]/page.tsx` (`EstimatePanel` only). Note: the value-axis marker (≈45% of the way up $5k–$435k) and the rank-based percentile (88%) legitimately differ because the comp prices are right-skewed (a few very expensive comps stretch the high to $435k); the bar is explicitly a price-value axis (low–median–high), so positioning the marker by value is the consistent, honest choice and matches its own labels. _(cycle: estimate-price-bar)_
- **The "Cost to own" panel on an aircraft listing detail page now shows the one-time capital buy-in for each co-ownership share — not just the recurring monthly/annual cost.** Before, the panel only answered "what does it cost to *run* my share each month"; the single biggest number a buyer faces — the capital to *get in* — was missing. Now the featured selected-share card shows the buy-in beneath the $/mo (e.g. "$429,900 to buy (the asking price)" for a sole owner, or "≈ $143,300 one-time to buy in (asking price ÷ 3)" for a 1/3 share), and the comparison table gains a **Buy-in** column showing the per-share capital for all four rows (sole / 1/2 / 1/3 / 1/4 = price ÷ 1,2,3,4). So a shopper sees the full "~$X to buy in + ~$Y/mo to run it" picture at a glance. Pure arithmetic on the real `asking_price` (`Math.round(price / shares)`) — no fabrication; the caveat copy clearly frames the buy-in as the asking price split evenly, "the real figure is negotiated." 3 files: `src/lib/calculators.ts` (added `buyInPerShare` to `ShareCostRow` + `estimateShareCosts`), `src/components/ShareCostPanel.tsx` (featured card line + table column + caveat copy), `src/lib/calculators.test.ts` (unit assertion). This was an in-flight branch from a prior cycle (spec + implementation present but uncommitted on staging — the prior process died before landing); this cycle committed it to `night/share-buyin-capital`, re-ran the full build + QA gate clean, and landed it. _(cycle: share-buyin-capital)_
- **The "How this stacks up" (Deal Score) panel on aircraft listing pages now opens with an at-a-glance tally of the signals it already lists** — e.g. a green pill reading "1 in this listing's favor" (and, when present, an amber "N to ask about"). The panel already synthesized our proprietary buyer signals (comp price position, days-on-market, price drops, spec completeness) into a transparent list of reasons, but a buyer had to scan and mentally count the rows to form a verdict. This cycle adds a small summary header above the existing list that counts the favorable rows (`kind === 'positive'`) and the watch-out rows (`kind === 'negative'`) — **neutral/context rows (e.g. "listed 2 weeks ago") are excluded** so the tally never overstates the case either way. The copy is descriptive, not a grade or score ("in this listing's favor" / "to ask about"), matching the panel's existing honesty caveats. Favorable-only listings show only the green pill; watch-out-only show only the amber; both show both; the panel still self-suppresses entirely when fewer than 2 signals exist (unchanged). No new data, query, score, or claim — purely a synthesis of rows the panel already computed and shows. 1 file: `src/app/aircraft/listing/[id]/page.tsx` (`DealScorePanel` only). _(cycle: deal-score-signal-tally)_
- **The ClubHanger Estimate now tells the buyer where this listing sits within the low–high spread of comparable asking prices** — e.g. "…3 other Cessna 172 listings for sale now range $49,995–$435,000 (median $125,000) — this one is **priced above 87% of them**." Last cycle (`estimate-price-range`) added the raw low–high range; this turns that static spread into a position signal so a shopper instantly sees relative expensiveness, not just the bounds. New `percentile` field on `ClubHangerEstimate` = the whole percent of the OTHER same-family priced comps that are priced strictly below this listing (0 = the cheapest in its family, ~100 = the priciest). It's computed from the exact same already-sorted `others` array used for the median/low/high — **no new query, no new DB read, no fabrication** (just a count over real listing prices). The panel renders it only in the range branch (`high > low`): the cheapest reads "priced below all of them", the priciest "priced above all of them", otherwise "priced above N% of them"; the degenerate single-value-spread fallback caption is unchanged. 3 files: `src/lib/aircraftEstimate.ts` (interface + compute + both return branches), `src/app/aircraft/listing/[id]/page.tsx` (EstimatePanel caption clause), `src/lib/aircraftEstimate.test.ts` (percentile assertions: cheapest→0, priciest→100, mid-spread→60). _(cycle: estimate-spread-position)_
- **The ClubHanger Estimate now publishes the low–high asking-price RANGE of the comparable same-family listings alongside the median**, so a seller/buyer sees "X other Cessna 172 listings for sale now range $49,995–$435,000 (median $Y)" instead of just the median. The range is the true min/max of the exact OTHER same-family priced comp set already used for the median and compCount — honest (real listings, whole dollars, no fabrication), proprietary (synthesized from our data; Controller/Barnstormers/Trade-A-Plane don't surface it), and needs **no new query**: the `others` array is already computed and sorted ascending in `clubHangerEstimate`, so `low = others[0]` / `high = others[last]`. Added `low`/`high` to the `ClubHangerEstimate` interface and to BOTH return branches (the `around` dead-band branch and the `below`/`above` branch). `EstimatePanel` renders the range in the existing caption and falls back to the prior median-only sentence when `low == high` (degenerate single-value spread → no "$X–$X" non-range). 3 files: `src/lib/aircraftEstimate.ts` (interface + both returns), `src/app/aircraft/listing/[id]/page.tsx` (EstimatePanel caption), `src/lib/aircraftEstimate.test.ts` (assert low/high on the below-market case). _(cycle: estimate-price-range)_
- **After clicking "Message" on a listing while logged out, signing in via magic link now takes you directly to the messaging thread — no need to click "Message" again.** Previously, `ContactBar`, `SeekerContactBar`, and `AircraftContactButton` all redirected anonymous users to `/auth?next=<listingPath>`. After auth, the user landed back on the listing page with their contact intent lost — they had to click "Message" a second time to actually open the thread. Now, the redirect encodes `?contact=1` in the `next` URL (e.g. `/auth?next=/partnerships/[id]%3Fcontact%3D1`). When the user returns to the listing page after signing in, a `useEffect` detects `?contact=1` and their authenticated session, auto-calls `getOrCreateThread` / `getOrCreateSeekerThread` / `getOrCreateAircraftThread`, and navigates to `/messages/[threadId]` — all within 1-2 seconds of page load. The `?contact=1` param is immediately removed from the URL via `history.replaceState` to prevent re-triggering on back-navigation. Owner-self-contact and null-posterId guards are preserved. 3 files changed: `src/components/ContactBar.tsx`, `src/components/SeekerContactBar.tsx`, `src/components/AircraftContactButton.tsx` (each: add `useRef`/`useSearchParams` imports; add `didAutoContact` ref; add auto-trigger `useEffect`; append `?contact=1` to the `next` URL in the unauthenticated redirect). _(cycle: contact-intent-restore)_
- **The partnerships browse page now supports a `?model=` URL filter, and the "Browse partnerships" CTA on aircraft listing pages now deep-links to exact-model partnership searches.** Previously, clicking "Browse Cessna 172 partnerships →" on an aircraft listing linked to `/partnerships?make=cessna`, showing all Cessna partnerships regardless of model. Now it links to `/partnerships?make=cessna&model=172`, showing only Cessna 172 co-ownership shares. The filter sidebar gains an "Aircraft Model" text input below "Aircraft Make" (appears in both desktop sidebar and mobile drawer). A removable filter chip for the model appears in `PartnershipActiveFilterChips` when `?model=` is set. The query layer (`getPartnershipListings`) applies the model as an ILIKE filter in both the mock-data and Supabase branches. 4 files changed: `src/lib/partnershipsQuery.ts` (add `model?: string` to `PartnershipFilters`; ILIKE in mock + Supabase branches); `src/components/PartnershipFilters.tsx` (Aircraft Model text input after Aircraft Make); `src/components/PartnershipActiveFilterChips.tsx` (model chip); `src/app/aircraft/listing/[id]/page.tsx` (CTA href uses `?make=X&model=Y` when model-level data exists, `?make=X` when make-level fallback). _(cycle: partnership-model-filter)_

---

## /partnerships — Browse partnerships  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/partnerships)

- **Fixed a silent loss of a saved listing across signup.** When a logged-out visitor hearted a listing, a soft-save prompt offered two paths: "Skip — save on this device" (which stored the listing locally) and the primary "Create a free account" button (which did **not**). So anyone who took the headline CTA — heart a plane → make an account to save it — came back signed in to an **empty heart**: the listing they signed up specifically to save was never persisted, so the device→account merge had nothing to merge. Now the "Create a free account" button writes the listing to the device store the instant it's clicked (synchronously, before the browser leaves for the sign-in page), so the already-existing `DeviceSaveSync` merge reconciles it into the brand-new account on return — the save the user asked for actually lands in `/saved`. Reuses the entire existing soft-save pipeline (`addLocalSave` + `mergeDeviceSaves`): **no schema, no new server action, no new query, no copy/layout change.** 2 files: `src/components/SoftSavePrompt.tsx` (new `onCreateAccount` prop fired on the CTA's `onClick`), `src/components/SaveListingButton.tsx` (passes `onCreateAccount={() => addLocalSave(...)}`). _(cycle: soft-save-keep-intent)_
- **The global-nav "Sign in" link now preserves a logged-out visitor's FULL current URL — including active query-string filters — across the auth round trip**, so a shopper mid-filter (e.g. `/aircraft?make=Cessna`) returns to their *exact filtered view* after signing in instead of the bare pathname. The prior `nav-signin-intent-next` cycle wired the nav Sign-in button to preserve the **pathname** via `?next=`, but explicitly left query-string filters out of scope because doing it with `useSearchParams` would force this layout-level nav (and therefore every page) into client/dynamic rendering and risk a CWV regression. Since `Nav` is already `'use client'`, this cycle reads `window.location.pathname + window.location.search` **at click time** in a new `handleSignInClick` handler: on a plain left-click it `preventDefault()`s, closes the mobile menu, and `router.push`es `/auth?next=<encoded full URL>` (or bare `/auth` on the homepage); modified clicks (cmd/ctrl/shift/alt or non-primary button) fall through to the native pathname-only `href={signInHref}` so new-tab / no-JS / SSR all still work. The handler is wired as `onClick` on both the desktop and mobile Sign-in `<Link>`s. **No `useSearchParams` is introduced — the nav/layout rendering mode is unchanged.** 1 file: `src/components/Nav.tsx`. _(cycle: nav-signin-filter-intent)_
- **The "Sign in" link in the top navigation bar now sends you back to the page you were on after you sign in, instead of dumping you on the homepage.** The nav's persistent Sign-in button (desktop and mobile menu, present on every page) previously pointed at a bare `/auth`, so a logged-out visitor browsing a listing or a search-results page who clicked "Sign in" lost their place and landed on the homepage default. It now carries `?next=<current page>` so the auth round trip returns the user where they were. On the homepage itself the link stays a plain `/auth` (no redundant `next=/`). _(cycle: nav-signin-intent-next)_
- **A logged-out visitor's "Save this search" now completes automatically after they sign in, instead of being silently dropped.** Before, clicking "Save this search" while signed out sent the user to `/auth?next=<filtered page>` and, on return, just re-rendered the button — they had to find and click it a second time, and many didn't. Now `SaveSearchButton` appends a `saveSearch=1` intent marker to the `next=` URL (mirroring the `?contact=1` auto-open idiom the message buttons already use). When the user lands back on the filtered page authenticated, a `useEffect` fires the save exactly once — ref-guarded so a re-render can't double-save, and the marker is stripped from the URL via `window.history.replaceState` so a page reload won't re-fire it. The saved query and its auto-generated name are computed from the active filters **minus** the marker, so `saveSearch=1` never leaks into the stored search. The signed-in direct-save path and the button's self-suppression when there are no active filters are unchanged. 1 file: `src/components/SaveSearchButton.tsx`. _(cycle: save-search-intent-restore)_
- **Partnership browse cards now show the actual magnitude of a buy-in comp — "~X% below market" / "~X% above market" — instead of a vague "Below market" / "Above market".** The partnership browse page already compared each listing's buy-in price to the median buy-in of OTHER active same-make partnerships (via `partnershipBuyInComp`, honesty-gated by ≥4 other priced comps and a ±5% dead-band), but it only rendered the direction, throwing away the percentage the comp function already computes. Now it surfaces the number, bringing the partnership browse card to parity with the aircraft browse card's CompPill ("~X% below average"). Below-market keeps the emerald (positive) treatment; above-market keeps amber. Honesty floors are unchanged: cards with too few comps or inside the ±5% dead-band still render NO chip (no fabricated or "~0%" chip — `pct` is guaranteed ≥1 by the comp function). 2 files changed: `src/components/PartnershipCard.tsx` (widen the `compVerdict` prop from `'below' | 'above'` to `{ kind, pct }`; render the percentage in both chips); `src/components/PartnershipList.tsx` (store `{ kind, pct }` in the `verdicts` map and thread it through `renderList`). No change to comp math, thresholds, or DB queries. _(cycle: partnership-comp-magnitude)_
- **The partnerships browse page now supports a `?model=` URL filter, and the "Browse partnerships" CTA on aircraft listing pages now deep-links to exact-model partnership searches.** Previously, clicking "Browse Cessna 172 partnerships →" on an aircraft listing linked to `/partnerships?make=cessna`, showing all Cessna partnerships regardless of model. Now it links to `/partnerships?make=cessna&model=172`, showing only Cessna 172 co-ownership shares. The filter sidebar gains an "Aircraft Model" text input below "Aircraft Make" (appears in both desktop sidebar and mobile drawer). A removable filter chip for the model appears in `PartnershipActiveFilterChips` when `?model=` is set. The query layer (`getPartnershipListings`) applies the model as an ILIKE filter in both the mock-data and Supabase branches. 4 files changed: `src/lib/partnershipsQuery.ts` (add `model?: string` to `PartnershipFilters`; ILIKE in mock + Supabase branches); `src/components/PartnershipFilters.tsx` (Aircraft Model text input after Aircraft Make); `src/components/PartnershipActiveFilterChips.tsx` (model chip); `src/app/aircraft/listing/[id]/page.tsx` (CTA href uses `?make=X&model=Y` when model-level data exists, `?make=X` when make-level fallback). _(cycle: partnership-model-filter)_

---

## /partnerships/[id]

- **Fixed a silent loss of a saved listing across signup.** When a logged-out visitor hearted a listing, a soft-save prompt offered two paths: "Skip — save on this device" (which stored the listing locally) and the primary "Create a free account" button (which did **not**). So anyone who took the headline CTA — heart a plane → make an account to save it — came back signed in to an **empty heart**: the listing they signed up specifically to save was never persisted, so the device→account merge had nothing to merge. Now the "Create a free account" button writes the listing to the device store the instant it's clicked (synchronously, before the browser leaves for the sign-in page), so the already-existing `DeviceSaveSync` merge reconciles it into the brand-new account on return — the save the user asked for actually lands in `/saved`. Reuses the entire existing soft-save pipeline (`addLocalSave` + `mergeDeviceSaves`): **no schema, no new server action, no new query, no copy/layout change.** 2 files: `src/components/SoftSavePrompt.tsx` (new `onCreateAccount` prop fired on the CTA's `onClick`), `src/components/SaveListingButton.tsx` (passes `onCreateAccount={() => addLocalSave(...)}`). _(cycle: soft-save-keep-intent)_
- **Clicking "Message" on a partnership listing or a member profile while logged out, then signing in, now drops you straight into the message thread — no need to click "Message" a second time.** The prior `contact-intent-restore` cycle fixed this for the mobile contact bar and the aircraft/seeker buttons, but missed the two **desktop** partnership components: `MessageOwnerButton` (the "Message {name}" button on seed-persona cards and on `/members/[id]`) and `ContactButtons` (the desktop card for real partnership listings). Both still sent logged-out users to `/auth?next=<listing>` with no intent signal, so after signing in they landed back on the listing with their intent lost. Now both append `?contact=1` to the redirect. On `/partnerships/[id]` the always-mounted `ContactBar` already auto-opens on `?contact=1`, so it stays the single trigger (no second `getOrCreateThread` call → no risk of a duplicate thread, since that action has no unique constraint). `MessageOwnerButton` also gained an opt-in `autoContactOnReturn` prop that mounts its own auto-open trigger — turned on only for `/members/[id]`, which has no ContactBar — so the standalone member-profile button works too. 3 files: `src/components/ContactButtons.tsx` (append `?contact=1`), `src/components/MessageOwnerButton.tsx` (append `?contact=1` + opt-in auto-open `useEffect` mirroring ContactBar), `src/app/members/[id]/page.tsx` (pass `autoContactOnReturn`). _(cycle: desktop-contact-intent-restore)_
- **After clicking "Message" on a listing while logged out, signing in via magic link now takes you directly to the messaging thread — no need to click "Message" again.** Previously, `ContactBar`, `SeekerContactBar`, and `AircraftContactButton` all redirected anonymous users to `/auth?next=<listingPath>`. After auth, the user landed back on the listing page with their contact intent lost — they had to click "Message" a second time to actually open the thread. Now, the redirect encodes `?contact=1` in the `next` URL (e.g. `/auth?next=/partnerships/[id]%3Fcontact%3D1`). When the user returns to the listing page after signing in, a `useEffect` detects `?contact=1` and their authenticated session, auto-calls `getOrCreateThread` / `getOrCreateSeekerThread` / `getOrCreateAircraftThread`, and navigates to `/messages/[threadId]` — all within 1-2 seconds of page load. The `?contact=1` param is immediately removed from the URL via `history.replaceState` to prevent re-triggering on back-navigation. Owner-self-contact and null-posterId guards are preserved. 3 files changed: `src/components/ContactBar.tsx`, `src/components/SeekerContactBar.tsx`, `src/components/AircraftContactButton.tsx` (each: add `useRef`/`useSearchParams` imports; add `didAutoContact` ref; add auto-trigger `useEffect`; append `?contact=1` to the `next` URL in the unauthenticated redirect). _(cycle: contact-intent-restore)_
- **After posting a partnership or seeking listing, the poster now sees a contextual green "Your listing is live!" confirmation banner on their new listing page.** Previously, `createPartnership` and `createSeekerListing` redirected silently to the detail page with no confirmation — users landed on their listing wondering "did it work?" Now both actions append `?posted=1` to the redirect. Partnership detail pages render a green banner: "Your partnership is live! Pilots searching for partnerships near {airport} can now find you. Browse pilots who are seeking →" (links to `/partnerships/seeking?airport={ICAO}`). Seeking detail pages render: "Your seeking listing is live! Aircraft owners looking for partners can now find you. Browse partnerships near {airport} →" (links to `/partnerships?airport={ICAO}`). Visiting either page without `?posted=1` shows no banner — existing visitor view is unchanged. 3 files changed: `src/app/actions.ts` (append `?posted=1` to both redirects); `src/app/partnerships/[id]/page.tsx` (add `searchParams` prop, `justPosted` flag, success banner JSX); `src/app/partnerships/seeking/[id]/page.tsx` (same pattern). _(cycle: post-publish-success)_
- **Partnership detail pages now show an "Engine Life" panel when the listing has SMOH and a known engine type.** The panel shows hours remaining to TBO with a colour-coded progress bar (emerald/amber/red), the matched engine family, and an engine reserve budget (estimated $/yr and $/hr based on typical overhaul cost spread over TBO at 100 hrs/yr). When the engine is past TBO, the panel shows an amber "Engine is beyond published TBO" warning with hours over-TBO. The panel self-suppresses when either `smoh` or `engine_type` is null, or when the engine string can't be matched to a known piston-GA family. The `Partnership` TypeScript interface was updated to include `ttaf`, `smoh`, and `engine_type` fields (these columns already existed in `schema.sql` and were fetched by the `select('*')` query; the TS type just hadn't been updated when the columns were added). Mock data updated to include realistic engine data for the four mock partnerships. 3 files changed: `src/app/partnerships/[id]/page.tsx` (import `computeEngineLife`/`EngineLifeResult` from `@/lib/engineLife`; add `Wrench`/`AlertTriangle` to lucide imports; compute `engineLife`; render `EngineLifePanel` inline after `PartnershipDealSignals`; add `money` helper + `EngineLifePanel` function at the bottom of the file); `src/lib/types.ts` (add `ttaf`, `smoh`, `engine_type` to `Partnership` interface); `src/lib/mockData.ts` (add engine fields to all four mock partnerships). _(cycle: partnership-engine-life)_

---

## /partnerships/new  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/partnerships/new)

- **Every "Post a…" form now has a one-click, guarded "Start over" control so a returning user whose autosaved draft was restored can clear it and begin a fresh listing without wiping each field by hand.** Autosave + restore already worked (`useFormDraft`), but a *restored* draft was a dead end if it was the wrong one (e.g. the user wants to list a different aircraft): they had to manually empty every field. This cycle adds a `reset()` callback to `useFormDraft` — it clears the debounce timer, drops the failed-submit snapshot, removes the localStorage draft key, calls native `form.reset()` (so the uncontrolled fields empty back to their HTML defaults — note `reset()` fires no input/change, so autosave does **not** re-arm), and sets `status` back to `idle`. All three Post forms (`PostAircraftForm`, `PostPartnershipForm`, `PostSeekerListingForm`) render a subtle `type="button"` "Start over" link beside the draft indicator, shown **only when a draft exists** (`status === 'saved' || 'restored'`) and hidden otherwise; clicking it `window.confirm()`s before wiping (guard against accidental data loss). 4 files: `src/components/useFormDraft.ts` (+`reset()`), and the three form components (wire `reset` → guarded `handleStartOver`, render the control). This was an in-flight branch from a prior cycle (spec + uncommitted implementation present on `night/draft-start-over`; the prior process died before landing); this cycle reviewed the diff against acceptance criteria, committed it, re-ran the full build + typecheck + QA gate clean, and landed it. _(cycle: draft-start-over)_
- **The photo uploader on the "Post a Partnership" form is now an always-visible section instead of being buried inside the collapsed "More details (optional)" disclosure** — bringing it to parity with the Sell-Your-Aircraft form, where the `aircraft-post-photos-visible` cycle already did the same. Photos are the single highest-value element of a listing (a buyer scrolls right past a photoless one), yet on `/partnerships/new` the uploader sat inside a `<details>` that defaults closed, so the fastest path to a compelling partnership listing was effectively invisible on page load. This cycle lifts the existing **Photos** block (heading + "Real photos make your listing far more compelling. Add up to 5." helper + `<PartnershipPhotoUpload />`) out of the "More details" disclosure into its own always-visible `<section>` placed between "The basics" and the "More details" disclosure, using the same card styling as the aircraft form's surfaced Photos section (`rounded-xl border border-slate-200 bg-white p-4 shadow-sm sm:p-6`). The uploader component, its upload endpoint, MAX_PHOTOS, and accepted types are all unchanged — the block is moved, not modified. "More details" still holds Year, Listing details (Title/Description), Ongoing costs, and Share structure, and stays collapsed by default. The AI-prefill `hasOptional` auto-open logic is untouched (it never referenced photos — photos aren't a form field). 1 file: `src/components/PostPartnershipForm.tsx`. _(cycle: partnership-post-photos-visible)_
- **The photo uploader on both post forms is now a persistent drop target and accepts clipboard paste.** `PartnershipPhotoUpload` (shared by `/aircraft/new` and `/partnerships/new`) previously rendered its drag-drop zone ONLY while `photos.length === 0`, so after the first photo landed the advertised fastest path — drag-drop — silently stopped working and the only way to add photos 2–5 was the small "+" tile → file browser. This cycle (1) lifts `onDragOver`/`onDragLeave`/`onDrop` from the empty-state zone to the **outer container**, so a drop anywhere on the component adds files whether or not photos already exist, with a subtle `ring-2 ring-sky-300` highlight shown once photos are present (the empty state keeps its big dashed zone); and (2) adds a document-level `paste` listener (mounted via `useEffect`, removed on unmount) that adds any image files from the clipboard — **guarded** to bail when an INPUT/TEXTAREA/SELECT/contentEditable is focused, so it never hijacks the "Prefill from your notes" AI textarea or any field. `addFiles` is already `useCallback`-memoized so the listener dep is stable. Respects `MAX_PHOTOS` (5) and the existing per-file slicing. Empty-state copy now reads "Drag or paste photos here, or browse"; the with-photos helper reads "{n}/{m} uploaded · drag, paste, or tap + to add more (up to 5)". 1 file: `src/components/PartnershipPhotoUpload.tsx`. No change to upload endpoints, storage, MAX_PHOTOS, accepted types, or the 5 MB limit. _(cycle: photo-upload-persistent-drop-paste)_
- **The N-number lookup and AI autofill now correctly fill the required Make field for aircraft whose make isn't one of the 8 dropdown presets (Maule, Aviat, Bellanca, Aeronca, American Champion, Socata, …) instead of silently leaving it blank.** The post forms have a fixed `MAKES` dropdown of 8 makes + "Other"; `matchMake` in `/api/faa-lookup` mapped the FAA manufacturer name to one of those 8 and returned `''` for anything else. The form then did `makeSelect.value = data.make` — but setting a `<select>` to a value with no matching `<option>` is a no-op, so Make (a **required** field) stayed blank even though the status read "Found: 1978 Maule M-5" and Model/Year filled in. The owner of any non-preset make hit a confusing blank-required-field / validation error. The AI prefill path had the same flaw (the LLM can return any make string). Fix: (1) `matchMake` now falls back to the registry's own make name in clean Title Case via a new `cleanMakeName` helper (e.g. "MAULE" → "Maule", "AVIAT AIRCRAFT INC" → "Aviat Aircraft" — title-cased + trailing corporate suffix dropped) instead of `''`; it never invents a name and still returns `''` only when the FAA gave no make at all. (2) Both forms gained a `fillMakeSelect` helper that injects an `<option>` (before "Other") for makes not already in the dropdown, then selects it — so the real make is stored (better data than blank/"Other") and the required field fills. Canonical makes (Cessna, Piper, …) still select their existing option with no duplicate injected. 3 files: `src/app/api/faa-lookup/route.ts` (add `cleanMakeName`; `matchMake` fallback + empty-input guard); `src/components/PostAircraftForm.tsx` and `src/components/PostPartnershipForm.tsx` (add `fillMakeSelect`; wire it into both the N-number-lookup and AI-prefill paths). _(cycle: nnumber-make-fallback)_
- **Partnership owners can now type their tail number at the top of the "Post a Partnership" form and have Make, Model, and Year fill in automatically.** Previously, the N-number field was buried inside the collapsed "More details" accordion with no lookup functionality — owners had to manually type Make (required select), Model (required input), and Year separately even though the FAA registry lookup already existed on the aircraft sale form. Now, an N-Number field is the first field in "The basics" section, above Make and Model. When the owner types a tail number (e.g. N12345) and tabs away, the form auto-calls `/api/faa-lookup`, fills the Make dropdown and Model input, opens "More details" to show the pre-filled Year, and shows a green "Found: {year} {make} {model}" confirmation. A "Look up →" button also triggers the lookup manually. The duplicate N-number field is removed from "More details". A `relatedTarget` guard prevents double-lookup when clicking the button directly from the input. One file changed: `src/components/PostPartnershipForm.tsx` (added `isLookingUp`/`lookupStatus` state + `handleLookup` async function; N-number field moved from "More details" to "The basics" as first field with blur auto-trigger + button; Year subsection simplified to single field; N-number removed from "Aircraft" subsection in "More details"). _(cycle: partnership-nnumber-autofill)_

---

## /aircraft — Planes for Sale (marketplace)  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/aircraft)

- **Fixed a silent loss of a saved listing across signup.** When a logged-out visitor hearted a listing, a soft-save prompt offered two paths: "Skip — save on this device" (which stored the listing locally) and the primary "Create a free account" button (which did **not**). So anyone who took the headline CTA — heart a plane → make an account to save it — came back signed in to an **empty heart**: the listing they signed up specifically to save was never persisted, so the device→account merge had nothing to merge. Now the "Create a free account" button writes the listing to the device store the instant it's clicked (synchronously, before the browser leaves for the sign-in page), so the already-existing `DeviceSaveSync` merge reconciles it into the brand-new account on return — the save the user asked for actually lands in `/saved`. Reuses the entire existing soft-save pipeline (`addLocalSave` + `mergeDeviceSaves`): **no schema, no new server action, no new query, no copy/layout change.** 2 files: `src/components/SoftSavePrompt.tsx` (new `onCreateAccount` prop fired on the CTA's `onClick`), `src/components/SaveListingButton.tsx` (passes `onCreateAccount={() => addLocalSave(...)}`). _(cycle: soft-save-keep-intent)_
- **The global-nav "Sign in" link now preserves a logged-out visitor's FULL current URL — including active query-string filters — across the auth round trip**, so a shopper mid-filter (e.g. `/aircraft?make=Cessna`) returns to their *exact filtered view* after signing in instead of the bare pathname. The prior `nav-signin-intent-next` cycle wired the nav Sign-in button to preserve the **pathname** via `?next=`, but explicitly left query-string filters out of scope because doing it with `useSearchParams` would force this layout-level nav (and therefore every page) into client/dynamic rendering and risk a CWV regression. Since `Nav` is already `'use client'`, this cycle reads `window.location.pathname + window.location.search` **at click time** in a new `handleSignInClick` handler: on a plain left-click it `preventDefault()`s, closes the mobile menu, and `router.push`es `/auth?next=<encoded full URL>` (or bare `/auth` on the homepage); modified clicks (cmd/ctrl/shift/alt or non-primary button) fall through to the native pathname-only `href={signInHref}` so new-tab / no-JS / SSR all still work. The handler is wired as `onClick` on both the desktop and mobile Sign-in `<Link>`s. **No `useSearchParams` is introduced — the nav/layout rendering mode is unchanged.** 1 file: `src/components/Nav.tsx`. _(cycle: nav-signin-filter-intent)_
- **The "Sign in" link in the top navigation bar now sends you back to the page you were on after you sign in, instead of dumping you on the homepage.** The nav's persistent Sign-in button (desktop and mobile menu, present on every page) previously pointed at a bare `/auth`, so a logged-out visitor browsing a listing or a search-results page who clicked "Sign in" lost their place and landed on the homepage default. It now carries `?next=<current page>` so the auth round trip returns the user where they were. On the homepage itself the link stays a plain `/auth` (no redundant `next=/`). _(cycle: nav-signin-intent-next)_
- **A logged-out visitor's "Save this search" now completes automatically after they sign in, instead of being silently dropped.** Before, clicking "Save this search" while signed out sent the user to `/auth?next=<filtered page>` and, on return, just re-rendered the button — they had to find and click it a second time, and many didn't. Now `SaveSearchButton` appends a `saveSearch=1` intent marker to the `next=` URL (mirroring the `?contact=1` auto-open idiom the message buttons already use). When the user lands back on the filtered page authenticated, a `useEffect` fires the save exactly once — ref-guarded so a re-render can't double-save, and the marker is stripped from the URL via `window.history.replaceState` so a page reload won't re-fire it. The saved query and its auto-generated name are computed from the active filters **minus** the marker, so `saveSearch=1` never leaks into the stored search. The signed-in direct-save path and the button's self-suppression when there are no active filters are unchanged. 1 file: `src/components/SaveSearchButton.tsx`. _(cycle: save-search-intent-restore)_

---

## /aircraft/[make] — Make-level for-sale pages  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/aircraft/new)

- **Every "Post a…" form now has a one-click, guarded "Start over" control so a returning user whose autosaved draft was restored can clear it and begin a fresh listing without wiping each field by hand.** Autosave + restore already worked (`useFormDraft`), but a *restored* draft was a dead end if it was the wrong one (e.g. the user wants to list a different aircraft): they had to manually empty every field. This cycle adds a `reset()` callback to `useFormDraft` — it clears the debounce timer, drops the failed-submit snapshot, removes the localStorage draft key, calls native `form.reset()` (so the uncontrolled fields empty back to their HTML defaults — note `reset()` fires no input/change, so autosave does **not** re-arm), and sets `status` back to `idle`. All three Post forms (`PostAircraftForm`, `PostPartnershipForm`, `PostSeekerListingForm`) render a subtle `type="button"` "Start over" link beside the draft indicator, shown **only when a draft exists** (`status === 'saved' || 'restored'`) and hidden otherwise; clicking it `window.confirm()`s before wiping (guard against accidental data loss). 4 files: `src/components/useFormDraft.ts` (+`reset()`), and the three form components (wire `reset` → guarded `handleStartOver`, render the control). This was an in-flight branch from a prior cycle (spec + uncommitted implementation present on `night/draft-start-over`; the prior process died before landing); this cycle reviewed the diff against acceptance criteria, committed it, re-ran the full build + typecheck + QA gate clean, and landed it. _(cycle: draft-start-over)_
- **The photo uploader on the Sell-Your-Aircraft form is now an always-visible section instead of being buried inside the collapsed "More details (optional)" disclosure.** Photos are the single highest-value element of a listing — a buyer scrolls past a photoless one — yet `/aircraft/new` hid the uploader behind a click on a disclosure that defaults closed, so the fastest path to a compelling listing was effectively invisible on page load. This cycle lifts the existing **Photos** block out of the `<details>` element into its own always-visible `<section>` placed between "The basics" section and the "More details" disclosure, using the same card styling as the basics card (`rounded-xl border border-slate-200 bg-white p-4 shadow-sm sm:p-6`) so the form stays cohesive. The uploader component (`PartnershipPhotoUpload`, endpoint `/api/upload-aircraft-photo`), MAX_PHOTOS, accepted types, and the More-details disclosure (still holding the Aircraft specs Year/TTAF/SMOH and Listing-details Title/Description blocks) are all unchanged. The N-number / AI-prefill auto-open logic for "More details" is untouched — its `hasOptional` check never referenced photos (photos aren't a form field), so moving the block out has no effect on it. 1 file: `src/components/PostAircraftForm.tsx`. _(cycle: aircraft-post-photos-visible)_
- **The photo uploader on both post forms is now a persistent drop target and accepts clipboard paste.** `PartnershipPhotoUpload` (shared by `/aircraft/new` and `/partnerships/new`) previously rendered its drag-drop zone ONLY while `photos.length === 0`, so after the first photo landed the advertised fastest path — drag-drop — silently stopped working and the only way to add photos 2–5 was the small "+" tile → file browser. This cycle (1) lifts `onDragOver`/`onDragLeave`/`onDrop` from the empty-state zone to the **outer container**, so a drop anywhere on the component adds files whether or not photos already exist, with a subtle `ring-2 ring-sky-300` highlight shown once photos are present (the empty state keeps its big dashed zone); and (2) adds a document-level `paste` listener (mounted via `useEffect`, removed on unmount) that adds any image files from the clipboard — **guarded** to bail when an INPUT/TEXTAREA/SELECT/contentEditable is focused, so it never hijacks the "Prefill from your notes" AI textarea or any field. `addFiles` is already `useCallback`-memoized so the listener dep is stable. Respects `MAX_PHOTOS` (5) and the existing per-file slicing. Empty-state copy now reads "Drag or paste photos here, or browse"; the with-photos helper reads "{n}/{m} uploaded · drag, paste, or tap + to add more (up to 5)". 1 file: `src/components/PartnershipPhotoUpload.tsx`. No change to upload endpoints, storage, MAX_PHOTOS, accepted types, or the 5 MB limit. _(cycle: photo-upload-persistent-drop-paste)_
- **The N-number lookup and AI autofill now correctly fill the required Make field for aircraft whose make isn't one of the 8 dropdown presets (Maule, Aviat, Bellanca, Aeronca, American Champion, Socata, …) instead of silently leaving it blank.** The post forms have a fixed `MAKES` dropdown of 8 makes + "Other"; `matchMake` in `/api/faa-lookup` mapped the FAA manufacturer name to one of those 8 and returned `''` for anything else. The form then did `makeSelect.value = data.make` — but setting a `<select>` to a value with no matching `<option>` is a no-op, so Make (a **required** field) stayed blank even though the status read "Found: 1978 Maule M-5" and Model/Year filled in. The owner of any non-preset make hit a confusing blank-required-field / validation error. The AI prefill path had the same flaw (the LLM can return any make string). Fix: (1) `matchMake` now falls back to the registry's own make name in clean Title Case via a new `cleanMakeName` helper (e.g. "MAULE" → "Maule", "AVIAT AIRCRAFT INC" → "Aviat Aircraft" — title-cased + trailing corporate suffix dropped) instead of `''`; it never invents a name and still returns `''` only when the FAA gave no make at all. (2) Both forms gained a `fillMakeSelect` helper that injects an `<option>` (before "Other") for makes not already in the dropdown, then selects it — so the real make is stored (better data than blank/"Other") and the required field fills. Canonical makes (Cessna, Piper, …) still select their existing option with no duplicate injected. 3 files: `src/app/api/faa-lookup/route.ts` (add `cleanMakeName`; `matchMake` fallback + empty-input guard); `src/components/PostAircraftForm.tsx` and `src/components/PostPartnershipForm.tsx` (add `fillMakeSelect`; wire it into both the N-number-lookup and AI-prefill paths). _(cycle: nnumber-make-fallback)_

---

## / — Homepage  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/)

- **The global-nav "Sign in" link now preserves a logged-out visitor's FULL current URL — including active query-string filters — across the auth round trip**, so a shopper mid-filter (e.g. `/aircraft?make=Cessna`) returns to their *exact filtered view* after signing in instead of the bare pathname. The prior `nav-signin-intent-next` cycle wired the nav Sign-in button to preserve the **pathname** via `?next=`, but explicitly left query-string filters out of scope because doing it with `useSearchParams` would force this layout-level nav (and therefore every page) into client/dynamic rendering and risk a CWV regression. Since `Nav` is already `'use client'`, this cycle reads `window.location.pathname + window.location.search` **at click time** in a new `handleSignInClick` handler: on a plain left-click it `preventDefault()`s, closes the mobile menu, and `router.push`es `/auth?next=<encoded full URL>` (or bare `/auth` on the homepage); modified clicks (cmd/ctrl/shift/alt or non-primary button) fall through to the native pathname-only `href={signInHref}` so new-tab / no-JS / SSR all still work. The handler is wired as `onClick` on both the desktop and mobile Sign-in `<Link>`s. **No `useSearchParams` is introduced — the nav/layout rendering mode is unchanged.** 1 file: `src/components/Nav.tsx`. _(cycle: nav-signin-filter-intent)_
- **The "Sign in" link in the top navigation bar now sends you back to the page you were on after you sign in, instead of dumping you on the homepage.** The nav's persistent Sign-in button (desktop and mobile menu, present on every page) previously pointed at a bare `/auth`, so a logged-out visitor browsing a listing or a search-results page who clicked "Sign in" lost their place and landed on the homepage default. It now carries `?next=<current page>` so the auth round trip returns the user where they were. On the homepage itself the link stays a plain `/auth` (no redundant `next=/`). _(cycle: nav-signin-intent-next)_

---

## /partnerships/seeking/[id]

- **After clicking "Message" on a listing while logged out, signing in via magic link now takes you directly to the messaging thread — no need to click "Message" again.** Previously, `ContactBar`, `SeekerContactBar`, and `AircraftContactButton` all redirected anonymous users to `/auth?next=<listingPath>`. After auth, the user landed back on the listing page with their contact intent lost — they had to click "Message" a second time to actually open the thread. Now, the redirect encodes `?contact=1` in the `next` URL (e.g. `/auth?next=/partnerships/[id]%3Fcontact%3D1`). When the user returns to the listing page after signing in, a `useEffect` detects `?contact=1` and their authenticated session, auto-calls `getOrCreateThread` / `getOrCreateSeekerThread` / `getOrCreateAircraftThread`, and navigates to `/messages/[threadId]` — all within 1-2 seconds of page load. The `?contact=1` param is immediately removed from the URL via `history.replaceState` to prevent re-triggering on back-navigation. Owner-self-contact and null-posterId guards are preserved. 3 files changed: `src/components/ContactBar.tsx`, `src/components/SeekerContactBar.tsx`, `src/components/AircraftContactButton.tsx` (each: add `useRef`/`useSearchParams` imports; add `didAutoContact` ref; add auto-trigger `useEffect`; append `?contact=1` to the `next` URL in the unauthenticated redirect). _(cycle: contact-intent-restore)_
- **After posting a partnership or seeking listing, the poster now sees a contextual green "Your listing is live!" confirmation banner on their new listing page.** Previously, `createPartnership` and `createSeekerListing` redirected silently to the detail page with no confirmation — users landed on their listing wondering "did it work?" Now both actions append `?posted=1` to the redirect. Partnership detail pages render a green banner: "Your partnership is live! Pilots searching for partnerships near {airport} can now find you. Browse pilots who are seeking →" (links to `/partnerships/seeking?airport={ICAO}`). Seeking detail pages render: "Your seeking listing is live! Aircraft owners looking for partners can now find you. Browse partnerships near {airport} →" (links to `/partnerships?airport={ICAO}`). Visiting either page without `?posted=1` shows no banner — existing visitor view is unchanged. 3 files changed: `src/app/actions.ts` (append `?posted=1` to both redirects); `src/app/partnerships/[id]/page.tsx` (add `searchParams` prop, `justPosted` flag, success banner JSX); `src/app/partnerships/seeking/[id]/page.tsx` (same pattern). _(cycle: post-publish-success)_

---

## /partnerships/seeking/new  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/partnerships/seeking/new)

- **Every "Post a…" form now has a one-click, guarded "Start over" control so a returning user whose autosaved draft was restored can clear it and begin a fresh listing without wiping each field by hand.** Autosave + restore already worked (`useFormDraft`), but a *restored* draft was a dead end if it was the wrong one (e.g. the user wants to list a different aircraft): they had to manually empty every field. This cycle adds a `reset()` callback to `useFormDraft` — it clears the debounce timer, drops the failed-submit snapshot, removes the localStorage draft key, calls native `form.reset()` (so the uncontrolled fields empty back to their HTML defaults — note `reset()` fires no input/change, so autosave does **not** re-arm), and sets `status` back to `idle`. All three Post forms (`PostAircraftForm`, `PostPartnershipForm`, `PostSeekerListingForm`) render a subtle `type="button"` "Start over" link beside the draft indicator, shown **only when a draft exists** (`status === 'saved' || 'restored'`) and hidden otherwise; clicking it `window.confirm()`s before wiping (guard against accidental data loss). 4 files: `src/components/useFormDraft.ts` (+`reset()`), and the three form components (wire `reset` → guarded `handleStartOver`, render the control). This was an in-flight branch from a prior cycle (spec + uncommitted implementation present on `night/draft-start-over`; the prior process died before landing); this cycle reviewed the diff against acceptance criteria, committed it, re-ran the full build + typecheck + QA gate clean, and landed it. _(cycle: draft-start-over)_

---

## /partnerships/seeking — Pilots seeking shares  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/partnerships/seeking)

- **A logged-out visitor's "Save this search" now completes automatically after they sign in, instead of being silently dropped.** Before, clicking "Save this search" while signed out sent the user to `/auth?next=<filtered page>` and, on return, just re-rendered the button — they had to find and click it a second time, and many didn't. Now `SaveSearchButton` appends a `saveSearch=1` intent marker to the `next=` URL (mirroring the `?contact=1` auto-open idiom the message buttons already use). When the user lands back on the filtered page authenticated, a `useEffect` fires the save exactly once — ref-guarded so a re-render can't double-save, and the marker is stripped from the URL via `window.history.replaceState` so a page reload won't re-fire it. The saved query and its auto-generated name are computed from the active filters **minus** the marker, so `saveSearch=1` never leaks into the stored search. The signed-in direct-save path and the button's self-suppression when there are no active filters are unchanged. 1 file: `src/components/SaveSearchButton.tsx`. _(cycle: save-search-intent-restore)_

---

## 🧪 Code-quality spot-checks — 1 judged, avg 4.0/5

- **deal-score-signal-tally — 4/5** — Chip color tokens are hardcoded literals rather than derived from the central SIGNAL_COLORS map (minor duplication); two filter passes over rows (negligible).

---

## To ship
Tell Claude "promote /aircraft" (or any pages above), or "promote everything." Claude merges the chosen work staging→main, which deploys to clubhanger.com.
