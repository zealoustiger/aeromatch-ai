## 2026-06-27T15:05:15Z — Night Shift run: 39 cycles (PASS 35 / FAIL 4) — reached run-until
- Models: cycles on sonnet; 4 escalated to sonnet; 9 quality-judged on sonnet

- PASS — seeking-post-prefill-all-fields — Seeker listing form AI box now prefills the entire form (preferred makes/models, category, year range, budget, airport, commute, pilot 
- PASS — similar-aircraft-deal-chips — honest Good deal / Priced high deal verdict chips added to Similar Aircraft rail cards on listing detail pages; batch-fetches family prices
- PASS — post-airport-autocomplete — airport autocomplete (city/IATA/ICAO → suggestions → picks ICAO) on /partnerships/new and /partnerships/seeking/new, replacing the raw IC
- PASS — partnership-market-check — ClubHanger Estimate analog for partnership buy-ins: a Partnership market check sidebar panel on `/partnerships/[id]` showing buyers whether th
- PASS — aircraft-post-airport-autocomplete — replaced the free-text Location + State fields on /aircraft/new with a single airport autocomplete (Based at), consistent with partn
- PASS — partnership-similar-deal-chips — Below market / Above market buy-in chips on the Similar Partnerships rail, mirroring what similar-aircraft-deal-chips did for aircraft l
- PASS — post-title-autofill — Title field made optional on aircraft + partnership post forms; server auto-generates from make/model/year when left blank, removing the last requi
- PASS — partnership-deal-signals — How this partnership stacks up synthesis panel added to partnership detail pages (Pillar 3 buyer-analysis)
- PASS — partnership-post-one-screen — Partnership post form collapsed to one smart screen: AI prefill at top, 5 required fields in a single The basics section, all optional fiel
- PASS — partnership-browse-comp-chips — comp price chips on /partnerships browse cards showing Below market / Above market buy-in badges using the same honesty-gated partnership
- PASS — seeking-post-one-screen — collapse seeking post form to one smart screen (Pillar 1: AI prefill at top, 3-field The basics visible above the fold, 7 sections collapse int
- cycle produced no verdict (exit 124)
- PASS — aircraft-post-one-screen — collapse aircraft post form to one smart screen
- PASS — avionics-panel — structured avionics & panel module on aircraft listing detail pages (Glass Panel / ADS-B Out / Autopilot / WAAS GPS capability chips + full equipment li
- PASS — avionics-browse-chips — aircraft browse cards now show Glass panel / ADS-B Out / Autopilot chips so IFR buyers can filter at a glance without opening every listing
- PASS — avionics-rail-chips — avionics capability chips now appear on AircraftRailCard (homepage curated rails + Similar aircraft rail on detail pages), completing coverage acro
- cycle produced no verdict (exit 124)
- PASS — my-listings-page — new `/listings` page gives sellers a post-publish home to find their aircraft and partnership listings, with My Listings added to both the profile dro
- PASS — avionics-partnership-detail — Avionics capability chips (Glass panel, ADS-B Out, Autopilot) now appear on partnership detail pages and rail cards, classified from descri
- cycle produced no verdict (exit 124)
- PASS — aircraft-post-success-redirect — after posting an aircraft, sellers now land on their own listing detail page with a Your listing is live! confirmation instead of the ge
- PASS — engine-time-browse-chips — engine TBO chips on aircraft browse cards: listings with SMOH + recognized engine type now show ~X hrs to TBO (or Beyond TBO) in the badge row
- cycle produced no verdict (exit 124)
- PASS — engine-time-rail-chips — engine TBO chips extended to homepage rails + Similar aircraft rail on listing detail pages, completing chip coverage across all aircraft card s
- PASS — defer-partnership-search-gate — removed the sign-up modal from homepage partnership search so logged-out visitors go straight to results
- PASS — deal-verdict-browse-chips — year+hours-controlled Good deal / Priced high chips on aircraft browse cards, falling back to CompPill when controlled comps are thin
- PASS — parts-wanted-filter — strengthened parts/WANTED title filter: 4 patterns → 9, now applied consistently across browse, Similar aircraft rail, and sitemap (previously mi
- PASS — alert-weekly-digest — weekly digest cron closes the email-alert loop: `GET /api/cron/alert-digest` (protected by `CRON_SECRET`, safe no-op without `RESEND_API_KEY`) pars
- PASS — deal-verdict-rail-cards — upgraded Similar aircraft rail to year+hours-controlled deal verdict; browse cards and similar-aircraft rail now show the same honest chip at e
- PASS — homerails-deal-chips — homepage curated rails now show year+hours-controlled Good deal / Priced high chips on aircraft cards, completing the deal verdict rollout across 
- PASS — contact-prefill — pre-fill contact email/name from user account on Partnership and Seeking post forms
- PASS — searches-onboarding-state — welcome onboarding state on /searches: new users landing after signup now see a 3-card orientation panel (Browse aircraft, Browse partnership
- PASS — share-cost-toggle — interactive share-split toggle on the cost-to-own panel on `/aircraft/listing/[id]`
- PASS — nnumber-autofill — moved N-number to top of aircraft post form with auto-trigger on blur, so typing a tail number and pressing Tab auto-fills Make/Model/Year without acc
- PASS — lazy-name-save — magic-link users who post a partnership or seeking listing now have their contact name silently saved to `user_metadata.full_name`, so the existing cont
- PASS — partnership-crosssell-listing — co-ownership cross-sell panel on aircraft listing detail pages: buyers viewing a for-sale aircraft now see a Co-ownership available sideb
- PASS — aircraft-post-price-location — Asking Price and Based-at Airport promoted to the always-visible The basics section of `/aircraft/new`, so sellers who use N-number autofi
- PASS — crosssell-model-level — Co-ownership available panel on aircraft listing detail pages now shows model-specific count (3 Cessna 172 partnerships) when same-model partners
- PASS — auth-context-headline — auth page now shows context-specific Sign in to post your aircraft listing / Sign in to contact the seller etc. headlines when users arrive mid-f


# Overnight review — 2026-06-27

## 📊 Traffic (PostHog) — as of 2026-06-27

- **Visitors:** 17 all-time · 12 in the last 7 days
- **Pageviews:** 522 all-time · 461 in the last 7 days
- **Not from Oakland:** 16 visitors _(early on, most non-local hits are crawlers/bots, not real users)_

**By city**

| City | Visitors | Pageviews |
|---|--:|--:|
| Oakland | 3 | 496 |
| (unknown) | 3 | 3 |
| Monte Vista | 2 | 2 |
| San Francisco | 1 | 8 |
| Seattle | 1 | 3 |
| El Cerrito | 1 | 2 |
| Atlanta | 1 | 1 |
| Wuhan | 1 | 1 |
| Council Bluffs | 1 | 1 |
| Fishkill | 1 | 1 |
| Bryant Pond | 1 | 1 |
| Singapore | 1 | 1 |

**Top pages**

| Page | Pageviews |
|---|--:|
| / | 116 |
| /aircraft | 88 |
| /partnerships | 68 |
| /admin | 49 |
| /admin/listings | 21 |
| /partnerships/seeking | 20 |
| /admin/backlog | 11 |
| /tools | 11 |
| /saved | 8 |
| /searches | 8 |

---

**38 cycles landed on staging across 11 pages.** Review the live staging site (you must be logged into Vercel), then tell Claude which pages to promote — or "promote everything."

Staging site: https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app

---

## /aircraft/[make]/[model] — Make + Model "for sale" pages

- **The "Cost to own" panel on aircraft listing detail pages is now interactive.** Previously the panel showed a static 4-row table (Full ownership / 1/2 / 1/3 / 1/4 share) with monthly and annual costs — buyers had to read all four rows and mentally focus on their scenario. Now four toggle pill buttons ("Sole owner", "1/2 share", "1/3 share", "1/4 share") sit at the top of the panel. Clicking one highlights that scenario with a prominent featured callout (large monthly + annual figures in a sky-50 callout box) and highlights the corresponding table row (sky-50 background, bold monthly figure). Table rows are also directly clickable. Default is "Sole owner." The underlying cost model (insurance ≈ 1% of price, hangar $7,500/yr, annual $2,500/yr, engine reserve when available) is unchanged. 2 files changed: new `src/components/ShareCostPanel.tsx` (Client Component — `useState` for selected share, toggle pills, featured callout, clickable table rows, same "Run your own numbers" + "Find a co-owner" links); `src/app/aircraft/listing/[id]/page.tsx` (import new component, remove inline `ShareCostPanel` function, remove unused `Wallet` import). _(cycle: share-cost-toggle)_
- **The "Similar aircraft for sale" rail on listing detail pages now shows the same year+hours-controlled "Good deal" / "Priced high" chips as browse cards**, instead of the less-accurate whole-family estimate it used before. Previously, the rail used `clubHangerEstimate` (comparing against the entire make+model family, no year or hours control) — a 1970 Cessna 172 listed below the family median (which includes modern avionics-equipped 172s) would falsely show "Good deal." Now the rail uses `clubHangerDealVerdict` (same as browse cards), which only awards a chip when ≥4 comps fall within ±5 years AND similar hours of the subject listing. Listings without year or ttaf show no chip. New `getFamilyCompsForBatch` helper in `aircraftForSale.ts` fetches id+price+year+ttaf for all active family comps in one query; each listing self-excludes from its own comp set in JS — no N+1 query problem. 2 files changed: `src/lib/aircraftForSale.ts` (new `getFamilyCompsForBatch`); `src/components/SimilarAircraft.tsx` (replace `getFamilyAskingPrices` + `clubHangerEstimate` with `getFamilyCompsForBatch` + `clubHangerDealVerdict`; self-exclusion by id; updated JSDoc). _(cycle: deal-verdict-rail-cards)_
- **Parts listings and WANTED ads from Barnstormers are now suppressed across every buyer-facing aircraft surface.** A new shared `src/lib/partsFilter.ts` module exports `PARTS_TITLE_PATTERNS` — 9 high-confidence ilike patterns (cowling, fairing, wheel pant, wheelpant, assembly, accepting orders, engine only, engine for sale, wanted) — applied uniformly as title-filter `.not()` chains. Previously the browse query (`AircraftSaleList.tsx`) had only 4 hardcoded patterns (wanted, wheelpant, wheel pant, assembly); the "Similar aircraft" rail (`getSimilarAircraftForSale`) and the sitemap (`getForSaleListingSitemapRows`) had **zero** title filtering — parts listings with prices ≥$50k could appear in the detail-page "Similar aircraft" module and generate their own sitemap entries (wasting Google crawl budget). 3 files changed: new `src/lib/partsFilter.ts` (pattern list with rationale); `src/components/AircraftSaleList.tsx` (replaces 4 hardcoded `.not()` calls with loop over shared constant); `src/lib/aircraftForSale.ts` (applies pattern loop in `getSimilarAircraftForSale` + `getForSaleListingSitemapRows`). _(cycle: parts-wanted-filter)_
- **Aircraft rail cards (homepage curated rails + "Similar aircraft for sale" rail on listing detail pages) now show an engine time-to-TBO chip in the photo overlay for listings where SMOH and engine type are both known.** The chip appears at bottom-left of the photo (bottom-2 left-2) — "~800 hrs to TBO" or "Beyond TBO" — colour-coded emerald (>50% TBO remaining), sky (15–50%), amber (<15% or beyond). Cards where `smoh` or `engine_type` is null show no chip. Cards with a placeholder photo continue to show "Not actual plane photo" at bottom-left (the chip is skipped there). The avionics chip at bottom-right is unaffected. 1 file changed: `src/components/AircraftRailCard.tsx` (import `lookupEngineTbo`; new `formatHrsRemaining`, `engineChipStyle`, `EngineOverlayChip` helpers; ternary replacing the `isPlaceholder` block to slot the engine chip when real photo + smoh + engine_type present). Mirrors the pattern already shipping on `AircraftSaleCard` (browse cards) — completing engine TBO chip coverage across all three aircraft surfaces: detail page Engine Life panel → browse cards → rail cards. _(cycle: engine-time-rail-chips)_
- **After posting an aircraft for sale, sellers are now redirected directly to their new listing's detail page instead of the generic browse page.** The detail page shows an emerald "Your listing is live!" notice (with a "View all my listings →" link to `/listings`) when `?posted=1` is present; the notice is absent on normal visits. Two files changed: `src/app/actions.ts` (one-line redirect change: `/aircraft?posted=${id}` → `/aircraft/listing/${id}?posted=1`); `src/app/aircraft/listing/[id]/page.tsx` (adds `searchParams` prop, reads `posted` param, renders inline success banner when present). _(cycle: aircraft-post-success-redirect)_
- **Aircraft rail cards (homepage curated collections + "Similar aircraft for sale" rail on listing detail pages) now show an avionics capability chip in the bottom-right of the photo overlay.** The top-priority cap (Glass panel > ADS-B Out > Autopilot > WAAS GPS > GPS navigator) renders as a colour-coded pill: violet for glass panel, sky for ADS-B Out, emerald for Autopilot. Cards with no extracted avionics data show no chip. 1 file changed: `src/components/AircraftRailCard.tsx` (import `classifyAvionics` + `cn`; `AVIONICS_CHIP_STYLE` map; `AvionicsOverlayChip` helper; compute `topAvionicsCap`; render chip in photo overlay at `bottom-2 right-2`). _(cycle: avionics-rail-chips)_
- **Aircraft listing pages now show an "Avionics & panel" module that classifies the extracted avionics list into buyer-facing capability chips.** When a listing has extracted avionics data, a new "AVIONICS & PANEL" section renders in the main column between the Specifications grid and the "How this stacks up" (Deal Score) panel. Detected equipment triggers colored capability chips: "Glass panel" (violet — G1000, G3x, Entegra, Avidyne, EFIS, etc.), "ADS-B Out" (sky — ADS-B, UAT, Stratus), "Autopilot" (emerald — S-TEC, GFC 500/700, KAP 140, etc.), "WAAS GPS" (sky — GTN series, GNS 430W/530W, GNC 355, etc.), and "GPS navigator" (slate — non-WAAS fallback). The full equipment list renders below the chips in a 2-column bullet grid. A caveat line notes the list is extracted from the seller's description and should be verified with logbooks. The avionics row is removed from the Specifications grid to avoid duplication. The panel self-suppresses completely when no avionics data was extracted. 2 files changed: new `src/lib/avionicsClassify.ts` (pure pattern-match classifier, 110 lines); `src/app/aircraft/listing/[id]/page.tsx` (import + classify call + remove avionics from specs grid + AvionicsPanel component + render call). _(cycle: avionics-panel)_
- **The "Similar aircraft for sale" rail on listing detail pages now shows honest "Good deal" or "Priced high" chips on each card, so buyers can compare price positioning across similar listings at a glance.** Cards for listings priced below the family-wide median (≥4 comps, outside the ±5% dead band) get an emerald "Good deal" chip; cards priced above get an amber "Priced high" chip; cards near the median or with thin comp data show no chip. The verdict is derived from the same `clubHangerEstimate` logic as the main detail-page ClubHanger Estimate panel — family asking prices are batch-fetched per unique make+model family in parallel (typically 1-3 families for same-make similar aircraft), so there are no N+1 queries. Existing homepage deals-rail `discountPct` chip is unchanged. 2 files changed: `SimilarAircraft.tsx` (imports `resolveMakeModelFamily` + `getFamilyAskingPrices` + `clubHangerEstimate`; builds family price map in parallel; passes `compVerdict` to each card), `AircraftRailCard.tsx` (new `compVerdict?: 'below' | 'above'` prop; emerald/amber chip renders on photo when set; `discountPct` takes precedence for deals rail). _(cycle: similar-aircraft-deal-chips)_
- **Aircraft listing pages now show a "How this stacks up" panel that synthesizes up to four buyer-relevant signals into one scannable card.** The panel appears in the main content column between the Specifications grid and the Engine Life panel. It shows: (1) price positioning — "Good deal / Fair price / Priced high" from the Deal Check (year+hours-controlled comps) or, when that's unavailable, the family-wide ClubHanger Estimate; (2) days on market — derived from `first_seen_at`, labelled "Fresh to market" (≤3 days), "Listed N days/months ago" (up to 30 days), or "Listed N months ago — seller may have flexibility" (≥90 days); (3) price history — "Price reduced $X — a seller motivation signal" or "Price increased $X" when a real recorded change exists; (4) spec completeness — "Well documented / N/5 key specs" based on presence of year, TTAF, SMOH, engine type, and registration. Each row has a colored dot (emerald for positive, slate for neutral, amber for negative), a bold label, and a plain-language detail line explaining what the signal means for a buyer. The panel self-suppresses when fewer than 2 signals are actionable. No new DB queries — everything derives from data already fetched on the page. 1 file changed (197 lines, `computeDealSignals` helper + `DealScorePanel` component). _(cycle: deal-score-panel)_
- **Aircraft listing pages now show what the plane would cost as a 1/2, 1/3, or 1/4 partnership share.** The "Estimated cost to own" panel is replaced by a 4-row share-split table (Full ownership / 1/2 share / 1/3 share / 1/4 share) with Monthly and Annual cost columns. Fixed costs (insurance ≈ 1% of asking price, $7,500/yr hangar, $2,500/yr annual inspection, and the engine reserve from the Engine Life panel when available) are divided equally by the number of partners; fuel/oil operating costs stay the same per partner since each flies their own hours. A "Find a co-owner on ClubHanger →" CTA cross-sells the partnership marketplace. New `estimateShareCosts()` helper in `calculators.ts` returns ShareCostRow[] for N=1,2,3,4; `ShareCostPanel` replaces the old single-row cost section. 2 files changed. _(cycle: cost-to-own-share-split)_
- **Aircraft listing pages now show an "Engine Life" panel with hours to TBO and an annual engine reserve budget.** When a listing has both SMOH (hours since overhaul) and a recognisable engine type, a new panel appears in the main content column — below the Specs grid and above Price History — showing how many hours remain before the manufacturer's published TBO, a colour-coded progress bar (green → amber → red as the engine runs down), and a rule-of-thumb engine reserve budget (per year and per hour). When the engine is beyond TBO the panel shows a clear amber warning card instead. The panel self-suppresses completely when SMOH or engine type is missing, or when the engine type can't be matched to a known piston-GA family. 2 new files: `src/lib/engineLife.ts` (pure TBO lookup + compute helper, 25+ engine families curated from Lycoming/Continental/Rotax published TBOs); `src/lib/engineLife.test.ts` (15 unit tests, all pass). 1 file modified: the listing detail page. _(cycle: engine-life-panel)_

---

## /partnerships/new  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/partnerships/new)

- **Magic-link users who post a listing now have their name remembered for future post forms.** Previously, magic-link sign-in users had no `full_name` in their Supabase user metadata (Google OAuth populates this automatically; magic link does not), so the contact-prefill feature left the "Your Name" field blank on every post form visit — they had to retype it each time. Now, when a magic-link user submits a partnership or seeking listing with a non-empty contact name, the `createPartnership` / `createSeekerListing` server actions silently call `supabase.auth.updateUser({data:{full_name:...}})` to persist the name. On their next visit, the existing contact-prefill logic picks it up and pre-fills the field automatically. Google OAuth users and users who already have a display name are unaffected. Also adds a one-line hint under the name field ("We'll save your name for future listings.") when the user is logged in but has no saved name yet. 3 files changed: `src/app/actions.ts` (name-save logic in `createPartnership` + `createSeekerListing`); `src/components/PostPartnershipForm.tsx` (hint text); `src/components/PostSeekerListingForm.tsx` (conditional hint replacing static hint). _(cycle: lazy-name-save)_
- **Logged-in users on the "Post a Partnership" and "Post a Seeking" forms now see their account email and display name pre-filled in the Contact section.** Previously the contact_email and contact_name fields were blank with a "leave blank to use your account email" hint — useful but invisible. Now, when a logged-in user visits either form, their email is pre-filled in the Email field and their display name (from `user.user_metadata.full_name`) in the Your Name field. Fields are fully editable. The hint text updates to "Pre-filled from your account — edit if needed. Not shown publicly." when pre-filled, and retains the original hint for logged-out users. 4 files changed: `src/app/partnerships/new/page.tsx` + `src/app/partnerships/seeking/new/page.tsx` (pass `userEmail={user?.email}` and `userName={user?.user_metadata?.full_name}` as new props); `src/components/PostPartnershipForm.tsx` + `src/components/PostSeekerListingForm.tsx` (add `userEmail?`/`userName?` props; set as `defaultValue` on contact inputs; conditional hint text). _(cycle: contact-prefill)_
- **The "Post a Partnership" form is now one smart screen.** The AI prefill box ("Have notes? Fill the whole form in one shot ✨") moves to the very top — the fastest path is now the most visible. All 5 required fields (Make, Model, Home Airport, Share Type, Buy-In Price) are consolidated into a single compact "The basics" section. Everything optional — Year, N-Number, Photos, Title, Description, Monthly Fixed, Wet Rate, Share Structure, and Contact info — collapses into a "More details (optional)" disclosure closed by default, with a chevron toggle. When AI prefill populates optional fields, the disclosure auto-opens. The contact_email field inside "More details" is now optional: the server action falls back to `user.email` so logged-in users no longer need to type their email. 2 files changed: `PostPartnershipForm.tsx` (restructured JSX, ~350 lines); `actions.ts` (one-line contact_email fallback in createPartnership). _(cycle: partnership-post-one-screen)_
- **The Title field on both the "Sell Your Aircraft" and "Post a Partnership" forms is now optional — leave it blank and the server auto-generates one from make + model + year** (e.g. "2006 Cessna 182T" for aircraft, "Cessna 172S Partnership" for partnerships). If a title is provided it wins; if blank, the fallback is generated server-side in `createAircraftListing` and `createPartnership`. Both form inputs show an "(optional)" label annotation and a "Leave blank to auto-fill from make, model, and year" hint line. This removes the last required "write something" field on both forms — sellers who fill in make/model/year can now publish without composing a headline at all. 4 files changed: `actions.ts` (IIFE title fallback in both create actions); `PostAircraftForm.tsx` (remove `required`, update label + hint); `PostPartnershipForm.tsx` (remove `required`, update label + hint). _(cycle: post-title-autofill)_
- **The Home Airport field on both partnership posting forms now accepts city names, IATA codes, and partial ICAO input — a dropdown of ranked airport suggestions appears as you type, and clicking one fills the field with the correct 4-letter ICAO.** Previously both forms required users to know and type the exact ICAO code (e.g. KAUS), blocking anyone unfamiliar with the registry notation. Now typing "Austin", "AUS", or "KAU" shows matching airports; picking one sets the field. Direct ICAO entry still works unchanged. New reusable `AirportFormInput` component (uncontrolled, form-native) uses the same Supabase `airports` table query + ranking logic (by airport size and commercial status, heliports/seaplane bases filtered out) as the existing browse-filter autocomplete, but renders a real `<input name="...">` that is compatible with `useFormDraft` autosave/restore and AI prefill (`fillFormField` DOM dispatch). 3 files changed: new `AirportFormInput.tsx`; `PostPartnershipForm.tsx` + `PostSeekerListingForm.tsx` (import + replace plain Input with AirportFormInput in the Home Airport section). _(cycle: post-airport-autocomplete)_
- **The "Post a Partnership" form AI box now prefills the entire form — not just title and description — from pasted notes or an existing listing.** The violet box is relabeled "Prefill from your notes ✨" and its subtext now says "the AI will prefill the whole form (aircraft, share terms, costs, title, and description)." Pasting notes that mention an aircraft and partnership structure (e.g. "2004 Cessna 172S, G1000, based at KAUS, 1/3 share, $15k buy-in, $300/mo fixed, $85/hr wet") populates make, model, year, N-number, home airport (ICAO), share type, total/available shares, buy-in price, monthly fixed cost, and wet rate — in addition to title + description. The model only extracts facts explicitly present in the input; fields not found in the input are left blank (no fabrication). The "Generating…" button label is now "Prefilling…" while active. `generatePartnershipDraft` in `actions.ts` now returns a `PartnershipDraft` interface with all 13 optional structured fields; `PostPartnershipForm.tsx` gains a `fillFormField` helper that sets each field's value and fires the appropriate DOM event (`'change'` for selects, `'input'` for text/number inputs). 2 files changed. _(cycle: partnership-post-prefill-all-fields)_
- **Anyone can now see and fill both partnership posting forms without an account.** Previously, visiting `/partnerships/new` or `/partnerships/seeking/new` without being logged in triggered a middleware 307 redirect to the sign-in page — so prospective posters couldn't even see the forms. Now both forms render for everyone. A blue guest banner ("Sign in to publish — your progress saves automatically on this device.") and a "Sign in to Publish →" submit button signal what happens next. When a guest clicks Submit, the draft is force-saved to localStorage and the browser navigates to `/auth?next=<form-path>`; after signing in, the form restores every field from the saved draft. Logged-in users: identical behaviour to before. 5 files changed: `src/proxy.ts` (removed `/partnerships/new` and `/partnerships/seeking/new` from `PROTECTED_PATHS`); `src/app/partnerships/new/page.tsx` + `src/app/partnerships/seeking/new/page.tsx` (now async server components that pass `isLoggedIn={!!user}` to their form); `src/components/PostPartnershipForm.tsx` + `src/components/PostSeekerListingForm.tsx` (new `isLoggedIn` prop; `forceSaveDraft` helper; `onFormSubmit` intercepts guest submit; guest banner + button text swap). _(cycle: partnership-post-deferred-gate)_

---

## /aircraft/[make] — Make-level for-sale pages  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/aircraft/new)

- **Sellers on the "Sell Your Aircraft" form can now type their tail number at the top of the form and have Make, Model, and Year fill in automatically.** Previously, the N-number (registration) field and its "Look up →" button were buried inside the collapsed "More details" accordion — sellers still had to manually type Make and Model (required fields) even though the FAA registry lookup already existed. Now the N-number field is the first field in "The basics" section, above Make and Model, with placeholder text "auto-fills make, model & year." When the seller types a tail number (e.g. N12345) and tabs away, the form automatically calls `/api/faa-lookup`, fills the Make dropdown and Model input, and opens "More details" to show the pre-filled Year — no button click needed. The "Look up →" button remains for explicit re-lookup. A `relatedTarget` guard prevents a double lookup when the button is clicked directly from the input. One file changed: `src/components/PostAircraftForm.tsx` (N-number row moved from "More details" to "The basics"; `onBlur` auto-trigger added; `data-lookup` guard on button; N-number removed from the "More details" accordion). _(cycle: nnumber-autofill)_
- **After posting an aircraft for sale, sellers are now redirected directly to their new listing's detail page instead of the generic browse page.** The detail page shows an emerald "Your listing is live!" notice (with a "View all my listings →" link to `/listings`) when `?posted=1` is present; the notice is absent on normal visits. Two files changed: `src/app/actions.ts` (one-line redirect change: `/aircraft?posted=${id}` → `/aircraft/listing/${id}?posted=1`); `src/app/aircraft/listing/[id]/page.tsx` (adds `searchParams` prop, reads `posted` param, renders inline success banner when present). _(cycle: aircraft-post-success-redirect)_
- **The "Sell Your Aircraft" form is now one smart screen.** The violet "Prefill from your notes ✨" AI box moves to the very top — the fastest path is most visible. "The basics" shows only two required fields: Make and Model. Everything optional — Year, N-Number + FAA lookup, TTAF, SMOH, Photos, Title, Description, Asking Price, Based at — collapses into a "More details (optional)" disclosure closed by default with a chevron toggle. When AI prefill or N-number lookup populates optional fields, the disclosure auto-opens. A seller can pick Make + Model and hit "Post Aircraft for Sale" without ever opening "More details." 1 file changed: `PostAircraftForm.tsx` (AI prefill at top, single "The basics" section, `<details>` disclosure for all optional fields, `detailsRef` auto-open on AI/FAA fill). _(cycle: aircraft-post-one-screen)_
- **The Title field on both the "Sell Your Aircraft" and "Post a Partnership" forms is now optional — leave it blank and the server auto-generates one from make + model + year** (e.g. "2006 Cessna 182T" for aircraft, "Cessna 172S Partnership" for partnerships). If a title is provided it wins; if blank, the fallback is generated server-side in `createAircraftListing` and `createPartnership`. Both form inputs show an "(optional)" label annotation and a "Leave blank to auto-fill from make, model, and year" hint line. This removes the last required "write something" field on both forms — sellers who fill in make/model/year can now publish without composing a headline at all. 4 files changed: `actions.ts` (IIFE title fallback in both create actions); `PostAircraftForm.tsx` (remove `required`, update label + hint); `PostPartnershipForm.tsx` (remove `required`, update label + hint). _(cycle: post-title-autofill)_
- **The "Sell Your Aircraft" form now has a single "Based at (optional)" airport autocomplete field instead of two separate free-text Location and State boxes.** Sellers type a city name, IATA code, or partial ICAO (e.g. "Austin", "AUS", or "KAUS") and a ranked dropdown shows matching airports; picking one sets the field to the 4-letter ICAO code. The server action looks up the ICAO in the airports table to derive the city and state automatically — accurate, standardised data stored without the seller needing to know the 2-letter state code. The AI "Prefill from your notes" box also extracts the airport ICAO from pasted text and fills the field. Consistent with the same AirportFormInput component already used on /partnerships/new and /partnerships/seeking/new. 2 files changed: `PostAircraftForm.tsx` (import + replace Location/State grid with AirportFormInput named home_airport; update handleGenerate to fill home_airport); `actions.ts` (createAircraftListing reads home_airport → airports table lookup → derives location/state; generateAircraftDraft schema updated to return home_airport ICAO instead of location/state text). _(cycle: aircraft-post-airport-autocomplete)_
- **The "Post an Aircraft" form AI box now prefills the entire form — not just title and description — from pasted listing text or notes.** The violet "Generate with AI ✨" box is relabeled "Prefill from your notes ✨" and now populates make, model, year, registration (N-number), total time (TTAF), SMOH, asking price, location, and state — in addition to title and description — from a single paste. The model only extracts facts explicitly present in the input (no fabrication); fields not found in the input are left blank. The `generateAircraftDraft` server action now returns an `AircraftDraft` interface with all these optional fields; the client fills each via DOM dispatch (same pattern as the N-number autofill). 2 files changed: `src/app/actions.ts` (extended return type + tool schema + system prompt); `src/components/PostAircraftForm.tsx` (new `fillFormField` helper, extended `handleGenerate`, updated subtext). _(cycle: aircraft-post-prefill-all-fields)_
- **Anyone can now see and fill the "Sell Your Aircraft" form without an account.** Previously, visiting `/aircraft/new` without being logged in immediately redirected to the sign-in page — so prospective sellers couldn't even see the form. Now the form renders for everyone. A blue guest banner ("Sign in to publish — your progress saves automatically on this device") and a "Sign in to Publish →" submit button signal what happens next. When a guest clicks Submit, the draft is force-saved to localStorage and the browser navigates to `/auth?next=/aircraft/new`; after signing in, the form comes back with every field restored from that saved draft. Logged-in users: identical behaviour to before. 2 files changed: `src/app/aircraft/new/page.tsx` (removed 3-line auth redirect; now passes `isLoggedIn` to the form); `src/components/PostAircraftForm.tsx` (new `isLoggedIn` prop; `forceSaveDraft` helper; `onFormSubmit` intercepts guest submit; guest banner + button text swap). _(cycle: aircraft-post-deferred-gate)_

---

## /partnerships/seeking/new  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/partnerships/seeking/new)

- **Magic-link users who post a listing now have their name remembered for future post forms.** Previously, magic-link sign-in users had no `full_name` in their Supabase user metadata (Google OAuth populates this automatically; magic link does not), so the contact-prefill feature left the "Your Name" field blank on every post form visit — they had to retype it each time. Now, when a magic-link user submits a partnership or seeking listing with a non-empty contact name, the `createPartnership` / `createSeekerListing` server actions silently call `supabase.auth.updateUser({data:{full_name:...}})` to persist the name. On their next visit, the existing contact-prefill logic picks it up and pre-fills the field automatically. Google OAuth users and users who already have a display name are unaffected. Also adds a one-line hint under the name field ("We'll save your name for future listings.") when the user is logged in but has no saved name yet. 3 files changed: `src/app/actions.ts` (name-save logic in `createPartnership` + `createSeekerListing`); `src/components/PostPartnershipForm.tsx` (hint text); `src/components/PostSeekerListingForm.tsx` (conditional hint replacing static hint). _(cycle: lazy-name-save)_
- **Logged-in users on the "Post a Partnership" and "Post a Seeking" forms now see their account email and display name pre-filled in the Contact section.** Previously the contact_email and contact_name fields were blank with a "leave blank to use your account email" hint — useful but invisible. Now, when a logged-in user visits either form, their email is pre-filled in the Email field and their display name (from `user.user_metadata.full_name`) in the Your Name field. Fields are fully editable. The hint text updates to "Pre-filled from your account — edit if needed. Not shown publicly." when pre-filled, and retains the original hint for logged-out users. 4 files changed: `src/app/partnerships/new/page.tsx` + `src/app/partnerships/seeking/new/page.tsx` (pass `userEmail={user?.email}` and `userName={user?.user_metadata?.full_name}` as new props); `src/components/PostPartnershipForm.tsx` + `src/components/PostSeekerListingForm.tsx` (add `userEmail?`/`userName?` props; set as `defaultValue` on contact inputs; conditional hint text). _(cycle: contact-prefill)_
- **The "Post a Seeking Listing" form is now one smart screen.** The AI prefill box ("Prefill from your notes ✨") moves to the very top — the fastest path is now the most visible. "The basics" section shows only three fields: Home Airport (required), Max Buy-In (optional), and Title (optional). Everything else — Aircraft Preferences, Budget, Location extras, Pilot Profile, Partnership Preferences, Description, and Contact Info — collapses into a "More details (optional)" disclosure closed by default with a chevron toggle. When AI prefill populates fields inside the closed section, the disclosure auto-opens. Title is now optional: the server action auto-generates "Pilot seeking [makes] partnership near [ICAO]" when left blank. contact_email is now optional: the server action falls back to user.email so logged-in users don't need to type it. 2 files changed: `PostSeekerListingForm.tsx` (restructured JSX, AI prefill at top, one-screen layout); `actions.ts` (title auto-gen + contact_email fallback in `createSeekerListing`). _(cycle: seeking-post-one-screen)_
- **The Home Airport field on both partnership posting forms now accepts city names, IATA codes, and partial ICAO input — a dropdown of ranked airport suggestions appears as you type, and clicking one fills the field with the correct 4-letter ICAO.** Previously both forms required users to know and type the exact ICAO code (e.g. KAUS), blocking anyone unfamiliar with the registry notation. Now typing "Austin", "AUS", or "KAU" shows matching airports; picking one sets the field. Direct ICAO entry still works unchanged. New reusable `AirportFormInput` component (uncontrolled, form-native) uses the same Supabase `airports` table query + ranking logic (by airport size and commercial status, heliports/seaplane bases filtered out) as the existing browse-filter autocomplete, but renders a real `<input name="...">` that is compatible with `useFormDraft` autosave/restore and AI prefill (`fillFormField` DOM dispatch). 3 files changed: new `AirportFormInput.tsx`; `PostPartnershipForm.tsx` + `PostSeekerListingForm.tsx` (import + replace plain Input with AirportFormInput in the Home Airport section). _(cycle: post-airport-autocomplete)_
- **The "Post a Seeking Listing" form AI box now prefills the entire form — not just title and description — from pasted notes.** The violet box is relabeled "Prefill from your notes ✨" and its subtext now says "the AI will prefill the whole form (aircraft preferences, budget, location, pilot profile, title, and description)." Pasting notes that mention aircraft preferences, budget, location, and pilot background (e.g. "IFR-rated, 450 hrs, fly 12 hrs/month out of KPAO, looking for 1/4 share in a Cessna 182 or SR22, max $20k buy-in, $400/mo, ~1hr commute") populates: preferred makes, preferred models, aircraft category, min/max year, max buy-in, max monthly, max hourly wet rate, home airport (ICAO), commute distance, total flight hours, ratings held, and hours per month — in addition to title + description. The model only extracts facts explicitly present in the input (no fabrication); fields not found in the input are left blank. The button label updates to "Prefilling…" while active. `generateSeekerDraft` in `actions.ts` now returns a `SeekerDraft` interface with all 15 optional structured fields; `PostSeekerListingForm.tsx` gains a `fillFormField` helper that sets each field's value and fires the appropriate DOM event. 2 files changed. _(cycle: seeking-post-prefill-all-fields)_
- **Anyone can now see and fill both partnership posting forms without an account.** Previously, visiting `/partnerships/new` or `/partnerships/seeking/new` without being logged in triggered a middleware 307 redirect to the sign-in page — so prospective posters couldn't even see the forms. Now both forms render for everyone. A blue guest banner ("Sign in to publish — your progress saves automatically on this device.") and a "Sign in to Publish →" submit button signal what happens next. When a guest clicks Submit, the draft is force-saved to localStorage and the browser navigates to `/auth?next=<form-path>`; after signing in, the form restores every field from the saved draft. Logged-in users: identical behaviour to before. 5 files changed: `src/proxy.ts` (removed `/partnerships/new` and `/partnerships/seeking/new` from `PROTECTED_PATHS`); `src/app/partnerships/new/page.tsx` + `src/app/partnerships/seeking/new/page.tsx` (now async server components that pass `isLoggedIn={!!user}` to their form); `src/components/PostPartnershipForm.tsx` + `src/components/PostSeekerListingForm.tsx` (new `isLoggedIn` prop; `forceSaveDraft` helper; `onFormSubmit` intercepts guest submit; guest banner + button text swap). _(cycle: partnership-post-deferred-gate)_

---

## / — Homepage  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/)

- **Homepage curated rails ("Browse curated collections") now show year+hours-controlled "Good deal" (emerald) or "Priced high" (amber) chips on aircraft cards** — the last aircraft surface without deal verdict chips. Previously, AircraftRailCards in HomeRails passed no `compVerdict`, so homepage rail cards showed no deal signal. Now HomeRails batch-fetches comps for every unique make+model family across all rail listings in one parallel `Promise.all` (one Supabase read per unique family, not per listing), computes `clubHangerDealVerdict` per listing self-excluding from its own comp set, and passes `compVerdict` to each `AircraftRailCard`. The `AircraftRailCard` component already supported `compVerdict` (added in `deal-verdict-rail-cards`); only HomeRails needed updating. Self-suppression is unchanged — listings with missing year/ttaf or thin comp sets (<4 within ±5 yr / similar hours) show no chip. 1 file changed: `src/components/HomeRails.tsx` (3 new imports; `FamilyKey`/`FamilySpec`/`BatchComp` types; early-return guard moved above comp-fetch; batch family-collect + `Promise.all` comp fetch + verdict-compute block; `compVerdict={verdicts.get(p.id)}` on each `AircraftRailCard`). _(cycle: homerails-deal-chips)_
- **Logged-out visitors who search for partnerships from the homepage are now taken directly to results — no sign-up modal.** Previously, clicking "Search" on the Partnerships tab showed a "Save your search?" modal (with email capture) before showing any listings; users had to click "Skip for now" to proceed. Now everyone navigates straight to `/partnerships?airport=…&radius=…`. The "Save this search" button on the results page handles email capture at the right moment. 1 file changed: `src/components/HeroSearch.tsx` (removed `user` auth state, `showGate`/`pendingParams` state, `SignUpGate` import, and the gating branch in `handleSearch()`; the function now always pushes to `/partnerships?…`). _(cycle: defer-partnership-search-gate)_
- **Aircraft rail cards (homepage curated rails + "Similar aircraft for sale" rail on listing detail pages) now show an engine time-to-TBO chip in the photo overlay for listings where SMOH and engine type are both known.** The chip appears at bottom-left of the photo (bottom-2 left-2) — "~800 hrs to TBO" or "Beyond TBO" — colour-coded emerald (>50% TBO remaining), sky (15–50%), amber (<15% or beyond). Cards where `smoh` or `engine_type` is null show no chip. Cards with a placeholder photo continue to show "Not actual plane photo" at bottom-left (the chip is skipped there). The avionics chip at bottom-right is unaffected. 1 file changed: `src/components/AircraftRailCard.tsx` (import `lookupEngineTbo`; new `formatHrsRemaining`, `engineChipStyle`, `EngineOverlayChip` helpers; ternary replacing the `isPlaceholder` block to slot the engine chip when real photo + smoh + engine_type present). Mirrors the pattern already shipping on `AircraftSaleCard` (browse cards) — completing engine TBO chip coverage across all three aircraft surfaces: detail page Engine Life panel → browse cards → rail cards. _(cycle: engine-time-rail-chips)_
- **Aircraft rail cards (homepage curated collections + "Similar aircraft for sale" rail on listing detail pages) now show an avionics capability chip in the bottom-right of the photo overlay.** The top-priority cap (Glass panel > ADS-B Out > Autopilot > WAAS GPS > GPS navigator) renders as a colour-coded pill: violet for glass panel, sky for ADS-B Out, emerald for Autopilot. Cards with no extracted avionics data show no chip. 1 file changed: `src/components/AircraftRailCard.tsx` (import `classifyAvionics` + `cn`; `AVIONICS_CHIP_STYLE` map; `AvionicsOverlayChip` helper; compute `topAvionicsCap`; render chip in photo overlay at `bottom-2 right-2`). _(cycle: avionics-rail-chips)_

---

## /aircraft — Planes for Sale (marketplace)  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/aircraft)

- **Parts listings and WANTED ads from Barnstormers are now suppressed across every buyer-facing aircraft surface.** A new shared `src/lib/partsFilter.ts` module exports `PARTS_TITLE_PATTERNS` — 9 high-confidence ilike patterns (cowling, fairing, wheel pant, wheelpant, assembly, accepting orders, engine only, engine for sale, wanted) — applied uniformly as title-filter `.not()` chains. Previously the browse query (`AircraftSaleList.tsx`) had only 4 hardcoded patterns (wanted, wheelpant, wheel pant, assembly); the "Similar aircraft" rail (`getSimilarAircraftForSale`) and the sitemap (`getForSaleListingSitemapRows`) had **zero** title filtering — parts listings with prices ≥$50k could appear in the detail-page "Similar aircraft" module and generate their own sitemap entries (wasting Google crawl budget). 3 files changed: new `src/lib/partsFilter.ts` (pattern list with rationale); `src/components/AircraftSaleList.tsx` (replaces 4 hardcoded `.not()` calls with loop over shared constant); `src/lib/aircraftForSale.ts` (applies pattern loop in `getSimilarAircraftForSale` + `getForSaleListingSitemapRows`). _(cycle: parts-wanted-filter)_
- **Aircraft browse cards now show a year- and hours-controlled "Good deal" or "Priced high" chip in the badge row.** The existing "~X% below/above average" CompPill compared a listing's price to the whole make+model family — a 1970 Cessna 172 would look "below market" simply because 2020s are in the same family. The new chip (`clubHangerDealVerdict`) first narrows the comp set to listings within ±5 years and similar total time (±1 000 hrs or 35%), then calls "Good deal" (emerald) or "Priced high" (amber). "Fair price" is suppressed. When the controlled comp set is thin (missing year/ttaf, or fewer than 4 matches), the card falls back to the existing CompPill — no regression. On the first browse page, 4 "Good deal" and 2 "Priced high" chips appeared; remaining cards showed CompPill or no price chip. 2 files changed: `AircraftSaleList.tsx` (new `fetchFamilyCompMap` fetching `id, year, ttaf` alongside prices; `clubHangerDealVerdict` computed per listing; `comp` nulled when deal verdict is available); `AircraftSaleCard.tsx` (new `dealVerdict` prop + `DealCheckChip` component; DealCheckChip renders instead of CompPill when verdict is present). _(cycle: deal-verdict-browse-chips)_
- **Aircraft browse cards now show an engine time-to-TBO chip in the badge row for listings where SMOH and engine type are both known.** The chip shows "~800 hrs to TBO" (or "~1.8k hrs to TBO" for larger values, rounded to the nearest 50 hrs), colour-coded by fraction of TBO remaining: emerald (> 50 % remaining — fresh major), sky (15–50 %), amber (< 15 % or beyond TBO). Cards where `smoh` or `engine_type` is null, or the engine type doesn't match a known piston-GA family, show no chip (correct self-suppression). On the first browse page, 12 listings displayed engine chips. Uses the same `lookupEngineTbo` table already powering the detail-page Engine Life panel — no new data source, no DB change. 1 file changed: `src/components/AircraftSaleCard.tsx` (import `lookupEngineTbo`; new `formatHrsRemaining`, `engineChipStyle`, `EngineTimeChip` helpers; render in badge row after avionics chips). _(cycle: engine-time-browse-chips)_
- **Aircraft browse cards now show avionics capability chips (Glass panel, ADS-B Out, Autopilot) so IFR buyers can identify equipped listings at a glance without clicking into each one.** Up to 2 top-priority chips appear in the existing badge row — violet "Glass panel" for G1000/Entegra/Avidyne/EFIS-equipped listings, sky-blue "ADS-B Out" for ADS-B-equipped listings, emerald "Autopilot" when an autopilot is detected. Priority order matches the detail-page AvionicsPanel (glass → ADS-B → autopilot → WAAS → GPS). Chips self-suppress on listings with no extracted avionics data. 1 file changed: `src/components/AircraftSaleCard.tsx` (import `classifyAvionics` + `AvionicsCap`, compute top-2 caps per card, render `AvionicsChip` helper in badge row after CompPill). _(cycle: avionics-browse-chips)_

---

## /partnerships/[id]

- **Partnership detail pages now show an "AVIONICS & PANEL" section with capability chips (Glass panel, ADS-B Out, Autopilot, WAAS GPS) classified from the listing description text.** The panel renders between the description and the "How this stacks up" deal-signals panel; it self-suppresses when no capabilities are detected. The PartnershipRailCard (similar-partnerships rail on detail pages) now shows a top-priority avionics overlay chip in the bottom-right of the photo, matching the pattern on AircraftRailCard. Both use the existing `classifyAvionics` keyword classifier with the description split into phrases as input, since the `partnerships` table has no structured `avionics[]` column (unlike `aircraft_for_sale`). The caveat reads "Capabilities mentioned in the listing description — verify with logbooks before purchase." 2 files changed: `src/app/partnerships/[id]/page.tsx` (Radio icon import; `classifyAvionics` import; description-phrase classification; `AvionicsPanel` inline component; `CAP_COLORS` map); `src/components/PartnershipRailCard.tsx` (classifyAvionics + AvionicsCap imports; `AVIONICS_CHIP_STYLE` map; `AvionicsOverlayChip` helper; description-phrase classification; chip render in photo overlay). _(cycle: avionics-partnership-detail)_
- **Partnership detail pages now show a "How this partnership stacks up" panel in the main content column, synthesising up to three buyer-relevant signals at a glance.** The panel appears after the description section and shows: (1) buy-in vs same-make median — "Buy-in below/around/above market" with dollar delta + pct + comp count (using the existing `partnershipBuyInComp` logic, only when ≥4 comps); (2) days listed — derived from `posted_at` / `created_at`, with contextual labels ("Just listed" / "Listed N days ago" / "Listed N months ago — seller may have flexibility"); (3) cost transparency — "Fully priced" (emerald) when buy-in + monthly + wet are all disclosed, "Partially priced" (neutral) when one rate is missing. Each signal has a colour-coded dot (emerald positive, slate neutral, amber negative) and a plain-language detail line. The panel self-suppresses when fewer than 2 signals are actionable. No new DB queries — uses only data already fetched on the page. 2 files changed: new `src/components/PartnershipDealSignals.tsx`; `src/app/partnerships/[id]/page.tsx` (import + render in main column). _(cycle: partnership-deal-signals)_
- **The "Similar partnerships" rail on partnership detail pages now shows "Below market" (emerald) or "Above market" (amber) buy-in chips on each card, so buyers can compare price positioning across similar listings at a glance.** Cards for partnerships with a buy-in price outside the ±5% dead band below the same-make median get an emerald "Below market" chip; those above get an amber "Above market" chip; cards near the median or with thin comp data show no chip. The verdict uses the same `partnershipBuyInComp` honesty gate as the PartnershipMarketCheck sidebar panel (≥4 other same-make comps required, ±5% dead band). Buy-in prices are batch-fetched per unique make in parallel with the IDs included so each listing is excluded from its own comp set. The PartnershipMarketCheck sidebar panel is unaffected. 2 files changed: `SimilarListings.tsx` (imports `partnershipBuyInComp`; batch-fetches buy-in prices per unique make; excludes each listing from its own comp set; passes `compVerdict` to each card); `PartnershipRailCard.tsx` (new `compVerdict?: 'below' | 'above'` prop; emerald/amber chip renders on photo when set, styled to match the `AircraftRailCard` deal chips). _(cycle: partnership-similar-deal-chips)_
- **Partnership detail pages now show a "Partnership market check" panel in the sidebar comparing the listing's buy-in to the median of other active same-make partnerships on ClubHanger.** The panel appears between the Costs card and the Cost Estimator when ≥4 other same-make active partnerships have a real buy-in price. It shows a verdict badge (Below market / Around market / Above market), a headline with the dollar and percent delta, the number of comps and their median buy-in, and a caveat noting that share types and partner counts vary. The panel self-suppresses completely when the listing has no buy-in price or when fewer than 4 same-make comps exist — it never fabricates or extrapolates. New `src/lib/partnershipComps.ts` pure helper (same honesty gates as `aircraftComps.ts`: MIN_OTHER_COMPS=4, DEAD_BAND=±5%); new `src/components/PartnershipMarketCheck.tsx` panel; `partnerships/[id]/page.tsx` gains a same-make buy-in fetch (limit 200, excludes current listing) + conditional render. 3 files changed. _(cycle: partnership-market-check)_

---

## (site-wide)

- **The email-alert subscriber loop is now complete.** Confirmed subscribers now receive a weekly digest when new matching listings exist. A new `GET /api/cron/alert-digest` route (called daily at 08:00 UTC by Vercel cron) queries all `status='confirmed'` alerts whose `last_digest_at` is older than 7 days (or null). For each, it parses the `source_path` (e.g. `/aircraft/cessna/172`, `/partnerships/near/khwd`, `/aircraft/for-sale/california`) into typed filter params, counts new aircraft or partnership listings since the subscriber's last digest, and if the count is > 0 sends a "N new {context} listings" email via `sendEmail` (no-ops safely if `RESEND_API_KEY` is absent) and stamps `last_digest_at`. Zero matches → alert skipped, `last_digest_at` unchanged (re-checked tomorrow). New `vercel.json` wires the daily cron schedule. New `buildAlertDigestEmail` in `src/lib/email.ts`. 3 files changed/created: `src/app/api/cron/alert-digest/route.ts` (new, 251 lines); `src/lib/email.ts` (add `buildAlertDigestEmail`); `vercel.json` (new cron config). **Human action needed:** set `CRON_SECRET` in the Vercel dashboard (Dashboard → Project → Settings → Environment Variables) and pass it in the Authorization header `Bearer $CRON_SECRET`; without it the route is unprotected (warning logged, not error). Also ensure `RESEND_API_KEY` and sender domain are verified in Resend before real emails go out. _(cycle: alert-weekly-digest)_
- **New "My Listings" page at `/listings` gives sellers a single place to find and navigate back to their posted aircraft and partnerships after publishing.** Queries `aircraft_for_sale` and `partnerships` by `poster_id = user.id`; shows title, status badge (Active/Pending), price, and date posted with a "View listing →" link per card. Empty state shows links to all three post forms ("Sell an aircraft", "Post a partnership", "Seeking a partnership"). Unauthenticated users redirect to `/auth?next=/listings`. "My Listings" added to the profile dropdown (desktop) and mobile nav menu between Messages and Saved. 3 files changed: new `src/app/listings/page.tsx`; `src/components/ProfileMenu.tsx` (new menu item); `src/components/Nav.tsx` (mobile menu item with Plane icon). _(cycle: my-listings-page)_

---

## /partnerships — Browse partnerships  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/partnerships)

- **Partnership detail pages now show an "AVIONICS & PANEL" section with capability chips (Glass panel, ADS-B Out, Autopilot, WAAS GPS) classified from the listing description text.** The panel renders between the description and the "How this stacks up" deal-signals panel; it self-suppresses when no capabilities are detected. The PartnershipRailCard (similar-partnerships rail on detail pages) now shows a top-priority avionics overlay chip in the bottom-right of the photo, matching the pattern on AircraftRailCard. Both use the existing `classifyAvionics` keyword classifier with the description split into phrases as input, since the `partnerships` table has no structured `avionics[]` column (unlike `aircraft_for_sale`). The caveat reads "Capabilities mentioned in the listing description — verify with logbooks before purchase." 2 files changed: `src/app/partnerships/[id]/page.tsx` (Radio icon import; `classifyAvionics` import; description-phrase classification; `AvionicsPanel` inline component; `CAP_COLORS` map); `src/components/PartnershipRailCard.tsx` (classifyAvionics + AvionicsCap imports; `AVIONICS_CHIP_STYLE` map; `AvionicsOverlayChip` helper; description-phrase classification; chip render in photo overlay). _(cycle: avionics-partnership-detail)_
- **Partnership browse cards now show "Below market" or "Above market" buy-in chips**, so buyers can spot deals and outliers at a glance without clicking into each listing. Cards with a buy-in price ≥4 same-make comps outside the ±5% dead band get an emerald "Below market" or amber "Above market" chip in the badges row. Cards with no buy-in, or thin comp data, or a buy-in within ±5% of the same-make median show no chip. `PartnershipList` batch-fetches buy-in prices per unique make in parallel, computes `partnershipBuyInComp` for each listing (excluding its own price from the comp set), and passes `compVerdict` to each `PartnershipCard`. 2 files changed: `PartnershipList.tsx` (batch-fetch + verdict map + pass prop); `PartnershipCard.tsx` (new `compVerdict` prop + emerald/amber chip in badges row using same `LineChart` icon as AircraftSaleCard CompPill). _(cycle: partnership-browse-comp-chips)_

---

## /searches  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/searches)

- **New users who land on "My Saved Searches" after signing up now see a welcoming orientation panel instead of a bare empty state.** Previously, the auth callback defaults to `/searches` when there's no `?next=` param — a brand-new user after magic-link or Google signup would see "No saved searches yet" with only small text links, with no explanation of what ClubHanger is or what to do next. Now, users with 0 saved searches see a "Welcome to ClubHanger" card above the instruction box with a one-line orientation blurb and three action cards: Browse aircraft for sale (→ `/aircraft`), Browse partnerships (→ `/partnerships`), Post a listing (→ `/partnerships/new`) — each with icon, description, and a coloured CTA. The existing "No saved searches yet" + instructional dashed box is preserved below. Users with 1+ saved searches see no change. 1 file changed: `src/app/searches/page.tsx` (adds `Plane, Users, PlusCircle, ArrowRight` to lucide imports; replaces the bare `!searches?.length` empty-state `<div>` with a `space-y-6` wrapper containing the welcome card + the original dashed instruction box). _(cycle: searches-onboarding-state)_

---

## /sitemap.xml  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/sitemap.xml)

- **Parts listings and WANTED ads from Barnstormers are now suppressed across every buyer-facing aircraft surface.** A new shared `src/lib/partsFilter.ts` module exports `PARTS_TITLE_PATTERNS` — 9 high-confidence ilike patterns (cowling, fairing, wheel pant, wheelpant, assembly, accepting orders, engine only, engine for sale, wanted) — applied uniformly as title-filter `.not()` chains. Previously the browse query (`AircraftSaleList.tsx`) had only 4 hardcoded patterns (wanted, wheelpant, wheel pant, assembly); the "Similar aircraft" rail (`getSimilarAircraftForSale`) and the sitemap (`getForSaleListingSitemapRows`) had **zero** title filtering — parts listings with prices ≥$50k could appear in the detail-page "Similar aircraft" module and generate their own sitemap entries (wasting Google crawl budget). 3 files changed: new `src/lib/partsFilter.ts` (pattern list with rationale); `src/components/AircraftSaleList.tsx` (replaces 4 hardcoded `.not()` calls with loop over shared constant); `src/lib/aircraftForSale.ts` (applies pattern loop in `getSimilarAircraftForSale` + `getForSaleListingSitemapRows`). _(cycle: parts-wanted-filter)_

---

## 🧪 Code-quality spot-checks — 8 judged, avg 4.0/5

- **avionics-partnership-detail — 3/5** ⚠️ — AC1's "raw equipment list in a 2-column bullet grid" is entirely absent from AvionicsPanel — only chips render; `CAP_COLORS`/`AVIONICS_CHIP_STYLE` maps are duplicated across `page.tsx` and `PartnershipRailCard.tsx` (with a minor `gps` shade mismatch); description-split regex is copied verbatim into both files instead of living in the shared lib.
- **share-cost-toggle — 4/5** — `TOGGLE_LABELS` duplicates labels already carried in `row.label` from the calculator (parallel map with no single source of truth); toggle buttons missing `type="button"` and `aria-pressed` (a11y gap — could misfire as submit inside any future form wrapper); `money()` copied from page.tsx rather than moved to a shared util.
- **engine-time-rail-chips — 4/5** — Three helpers (`formatHrsRemaining`, `engineChipStyle`, `EngineOverlayChip`) are near-verbatim copies of `AircraftSaleCard`'s equivalents with a different component name than the spec dictated (`EngineTimeChip`); duplication is spec-acknowledged but will compound if a third card type ever gains engine data.
- **seeking-post-one-screen — 4/5** — `result.title` remains in the `hasMoreDetails` auto-open guard even though `title` moved to "The basics" (outside the disclosure) — AI filling only a title triggers the disclosure to open for no reason; `contact_email: ... || ''` could persist an empty string to the DB if `user.email` is also null/undefined on an incomplete account.
- **partnership-post-one-screen — 4/5** — `scheduling_system` field silently dropped — it was in the old form, absent from the spec's more-details list AND the out-of-scope list, so it's an unacknowledged regression; auto-open heuristic also misses AI-filled contact fields (only checks year/registration/title/description/costs), so the drawer stays closed if only those populate.
- **similar-aircraft-deal-chips — 4/5** — Emerald chip markup is copy-pasted verbatim between the `discountPct` and `compVerdict='below'` branches — minor DRY miss that will drift if the deal-chip style is ever updated; `interface FamilySpec` and `type FamilyKey` are declared inside the async function body instead of at module scope (style nit).
- **partnership-post-prefill-all-fields — 4/5** — Outer `if (result.buy_in_price)` / `if (result.total_shares)` etc. guards before `fillFormField` calls introduce a falsy-0 bug — a numeric field set to 0 would be silently skipped; `fillFormField` already handles `undefined`/`null` internally, making the outer guards redundant and harmful for integers; low practical risk (a $0 buy-in is nonsensical) but it's a latent trap if the pattern is copied to a domain where 0 is meaningful.
- **homerails-deal-chips — 5/5**

---

## To ship
Tell Claude "promote /aircraft" (or any pages above), or "promote everything." Claude merges the chosen work staging→main, which deploys to clubhanger.com.
