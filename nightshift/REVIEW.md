# Overnight review — 2026-06-29

## 📊 Traffic (PostHog) — as of 2026-06-29

- **Visitors:** 31 all-time · 24 in the last 7 days
- **Pageviews:** 663 all-time · 546 in the last 7 days
- **Not from Oakland:** 29 visitors _(early on, most non-local hits are crawlers/bots, not real users)_

---

## 🧭 Visitors — day-over-day & week-over-week

_Real visitors (bots excluded), first-party, Pacific-day windows — matches the live `/admin` card._

- **Totals:** 6 yesterday _(vs 10 the day before)_ · 24 last 7 days _(vs 0 the prior 7)_

**Visitors by city**  ·  _Δ d/d = yesterday vs. the day before · Δ w/w = last 7 days vs. the prior 7_

| City | Yesterday | Δ d/d | Last 7d | Δ w/w |
|---|--:|:--|--:|:--|
| US | 2 | ▲ +1 | 3 | ▲ +3 |
| Unknown | 0 | ▼ −3 | 3 | ▲ +3 |
| Memphis, TN | 1 | — | 2 | ▲ +2 |
| Fishkill, NY | 0 | — | 2 | ▲ +2 |
| Arlington, VA | 1 | ▲ +1 | 1 | ▲ +1 |
| Ashburn, VA | 1 | ▲ +1 | 1 | ▲ +1 |
| Lithonia, GA | 1 | ▲ +1 | 1 | ▲ +1 |
| Auburn, WA | 0 | — | 1 | ▲ +1 |
| Bethel, ME | 0 | — | 1 | ▲ +1 |
| CA | 0 | — | 1 | ▲ +1 |

**Top landing pages**

| Page | Yesterday | Δ d/d | Last 7d | Δ w/w |
|---|--:|:--|--:|:--|
| / | 0 | ▼ −2 | 8 | ▲ +8 |
| /aircraft/listing/1350cd7e-6fa5-4c4d-bbb8-d5d6c1f77389 | 6 | ▲ +6 | 6 | ▲ +6 |
| /aircraft/for-sale/arkansas | 0 | ▼ −4 | 4 | ▲ +4 |
| /aircraft | 0 | — | 1 | ▲ +1 |
| /aircraft/listing/119eac1e-1ea0-4a77-8ef7-cf8417bc7f6a | 0 | — | 1 | ▲ +1 |
| /partnerships | 0 | ▼ −1 | 1 | ▲ +1 |
| /partnerships/browse | 0 | ▼ −1 | 1 | ▲ +1 |
| /partnerships/seeking | 0 | ▼ −1 | 1 | ▲ +1 |
| /partnerships/state/tx | 0 | ▼ −1 | 1 | ▲ +1 |

---

**35 cycles landed on staging across 10 pages.** Review the live staging site (you must be logged into Vercel), then tell Claude which pages to promote — or "promote everything."

Staging site: https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app

---

## /partnerships/[id]

- **The buy-in price on partnership detail pages now shows a compact inline market-comparison hint directly under the dollar amount.** Before, a buyer who noticed the "$18,000" buy-in had to scroll past the Costs card to see the separate PartnershipMarketCheck panel to get market context. Now a one-line hint appears right under the price: "~28% below market · $25k median · 4 comps" (emerald for below market), "Around market · $23k median · 4 comps" (slate for near market), or "~40% above market · $20k median · 4 comps" (amber for above market). Self-suppresses when fewer than 4 same-make partnerships have a buy-in price (honesty floor). The existing PartnershipMarketCheck sidebar panel is unchanged — the inline hint is additive, giving buyers the key signal at the price before they read the full explanation below. _(cycle: partner-buyin-inline-market)_
- **Posting a partnership or seeking listing now defaults to on-platform messaging instead of exposing the poster's email.** Both post forms gain "Message through ClubHanger (default)" as the first "Preferred Contact Method" option, ahead of the existing "Email only / Phone only / Email or phone" choices, and the server actions (`createPartnership`, `createSeekerListing`) now fall back to `contact_method='platform'` instead of `'email'` when nothing is submitted. The misleading "Not shown publicly" helper text under the email field — which was wrong for the `email`/`both` cases — is corrected to "Only shared when you select email contact above." Net effect: a new poster who doesn't actively choose email keeps their address private and routes buyers through the in-app Message thread, which both protects privacy and keeps engagement on-site. Existing listings are untouched (rows with `contact_method='email'` still render "Send Email"); `ContactButtons`/`SeekerContactBar` already suppress email/phone links and show only the Message button for `platform`, so no component change was needed. _(cycle: post-platform-default)_
- **Landed the partnership IFR suitability badge** (built and fully QA'd in the prior cycle but blocked by a stale `.next` build cache causing phantom-chunk 404s). The fix was clearing `.next` and rebuilding clean — the smoke gate passed immediately (6/6: HTTP 200, zero console errors, zero overflow). The feature: partnership detail pages now show a coloured IFR suitability verdict badge at the top of the Avionics & Panel section ("Glass panel — IFR touring setup", "IFR-capable: WAAS GPS + autopilot", etc.) so co-ownership buyers get the same synthesized IFR read that aircraft-for-sale listing pages already show. The shared `computeIfrSuitability` function was extracted from the aircraft page into `src/lib/avionicsClassify.ts` (12 unit tests) so both pages import one copy — aircraft behaviour unchanged. The prior FAIL's root cause was environmental: stale `.next` artifacts from earlier in the session caused the build to emit HTML referencing chunk filenames that weren't written to disk; a clean `rm -rf .next && npx next build` resolved it. _(cycle: avionics-ifr-land)_
- ⚠️ FAILED — Ported the honesty-gated **IFR-suitability buyer read** (shipped to aircraft listings in `avionics-ifr-summary`) onto the **partnership detail page's** Avionics panel, so co-ownership buyers get the same "can I file IFR in this shared aircraft, and how capable is the panel?" synthesis instead of a bare capability-chip list. Refactor: extracted `computeIfrSuitability` + `IfrTier`/`IfrSuitability` out of the aircraft page into the shared `src/lib/avionicsClassify.ts` (added `avionicsClassify.test.ts`, 12 unit tests) so both detail pages import one copy; aircraft behaviour unchanged. The code is complete and correct — **`npx next build` + typecheck pass, all 12 unit tests pass, and the rendered screenshots show the partnership avionics panel with the IFR badge + sub-line, zero horizontal overflow at 1280 + 375, HTTP 200.** _(cycle: partnership-avionics-ifr)_
- **The partnership detail sidebar now shows "Also near [AIRPORT]" — how many other active partnerships are within 100 nautical miles of this listing's home airport, with a direct link to browse them.** When viewing a Cessna 172 partnership at KPAO, a buyer now sees "19 other active partnerships within 100 nm of Palo Alto, CA → Browse all 19 nearby →" — immediately knowing whether this listing is the only option near their home field or one of many. The panel self-suppresses entirely when the airport has no other active partnerships within range (e.g., KAUS showed zero nearby, so the panel is hidden — no false promise of alternatives). The link goes to the existing `/partnerships/near/[icao]` route which already shows the same nearby listings in full. _(cycle: nearby-partnerships-panel)_
- **Seeking listings now appear on the "My Listings" management page, and the partnership and seeker post-success banners now include a "View all my listings →" link.** Before, pilots who posted a "seeking partnership" listing had no way to find or manage it on `/listings` — the page only showed aircraft and partnerships. After posting a seeker listing, a pilot visiting `/listings` now sees a "Pilots seeking (N)" section with status badge, title, home airport, and a "View →" link to their listing. Separately, the post-success banners on partnership and seeker detail pages (shown with `?posted=1`) now include a "View all my listings →" link to `/listings`, matching what the aircraft success banner already showed. _(cycle: seeker-on-listings-page)_
- **The "Partnership market check" sidebar panel on partnership detail pages now shows listing-age context below the buy-in comparison.** Before, the panel showed how the buy-in compared to the median of other same-make partnerships, but was silent on how long the listing had been active. Now the footer reads, e.g., "Listed 19 days ago — listed more recently than ~60% of the 5 comparable Cessna partnerships listed now" (when fresher than most), or "Listed 14 days ago — on the market longer than ~75% of the 8 comparable Cirrus partnerships listed now — seller may have flexibility" (when stale and ≥ 30 days old), or the plain "Listed X days ago" when there aren't enough comps with a known created_at to compare (honesty floor). Mirrors what `listing-age-context` shipped for the aircraft EstimatePanel. _(cycle: partner-listing-age-context)_
- **The "Flying cost" panel on partnership detail pages now shows an estimated engine overhaul reserve row below the cost breakdown — the missing piece of true co-ownership cost.** When SMOH and engine type are known for the partnership's aircraft, a dashed-border row appears: "Engine reserve est. (Lycoming IO-360) — +$1,300/yr" (or whatever the math computes). The figure updates live as the buyer tabs between 50/75/100/150 hrs/yr — flying more hours means accumulating reserve faster. The row is labeled "Verify if included in monthly fixed above" so buyers know to check whether the partnership's stated monthly rate already covers a reserve (avoiding double-counting). The reserve is NOT folded into the main highlighted total for the same reason — it supplements the stated figures rather than replacing them. Self-suppresses entirely when smoh or engine_type are absent (no fabricated reserve, no empty row). _(cycle: partner-cost-engine-reserve)_
- **Partnership detail pages now show a purpose-built "Flying cost" panel that answers the two questions every co-ownership buyer has: "What will it really cost me to fly this partnership annually?" and "Does the buy-in pay for itself vs. renting?"** The panel has four selectable tabs (50 / 75 / 100 / 150 hrs/yr); at each tab it shows the annual total, a per-month figure, and the true per-flight-hour cost — computed from the listing's actual `monthly_fixed` and `hourly_wet` rates. When the partnership works out cheaper than renting at $150/hr (a typical piston-GA club rate), an emerald callout shows the annual saving and how many years it takes to recoup the buy-in. When renting is cheaper at the selected utilization, a plain note explains that flying more hours brings partnership savings. The panel self-suppresses when both rates are null/zero (no empty card). The previous generic CostCalculator compact is replaced; the `/tools/cost-calculator` page is unaffected. _(cycle: partner-share-cost-panel)_
- **The "How this partnership stacks up" panel on partnership detail pages now shows two new signals: an engine-life read (when SMOH and engine type are known) and a spec-completeness row that tells buyers whether the engine/airframe data is on the listing or whether to ask for it.** Before, the partnership signal tally showed four rows (buy-in vs comps, implied value, days listed, cost transparency) but was silent about engine data — even though the Engine Life and Airframe panels lower on the page exist specifically to surface that data. Now the tally also shows "Engine has life left / Mid-time engine / Approaching TBO / Engine past TBO" (with the same green/amber/red bands as the aircraft DealScorePanel) when SMOH + engine type resolve a known TBO family; and always shows either "Engine specs on listing — SMOH and engine type provided" (positive) or "Engine/airframe specs not on listing — ask the owner for SMOH, engine type, and TTAF" (neutral) so buyers know what data to request. The engine-life row self-suppresses when the data is missing or unrecognised — no fabricated TBO. _(cycle: partnership-signals-engine-spec)_
- **The "How this partnership stacks up" panel on partnership detail pages now shows whether the implied full-aircraft value (buy-in × total shares) lines up with what the same make/model actually sells for on the open market.** Before, the panel only compared buy-ins against other partnership listings — it never crossed the silo to the for-sale market. Now, when a partnership has both a buy-in price and a total-shares count, the panel computes `buy_in × total_shares` and compares it to the median asking price of same make/model aircraft currently for sale on ClubHanger. On a 1/4-share Cirrus SR22 at $32,000, for example, the panel shows "Implied aircraft value looks favorable — 4 × $32,000 = $128,000 implied value, X% below the $Y median of N Cirrus SR22 aircraft for sale." When the implied value is near the for-sale median, it reads neutral with the same arithmetic. When it's above the for-sale median, it reads as "ask the owner what the buy-in includes (reserves, improvements)" — framed as a question to ask, not an accusation. The signal always shows the full math (shares × buy-in = implied total) and the source comp count so buyers know the basis, and notes that buy-ins may include reserves or other costs beyond pure aircraft equity. _(cycle: partnership-implied-value)_

---

## /aircraft/[make]/[model] — Make + Model "for sale" pages  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/aircraft/cessna/182)

- **Aircraft listing detail pages now show a compact inline market hint directly under the asking price in the sidebar Price card.** Before, a buyer who saw "$72,000" in the price card had to scroll down to the EstimatePanel to learn it was "~42% below market." Now the one-liner appears immediately: "~42% below market · $125k median · 66 comps" (emerald for below market), "Around market · $125k median · 66 comps" (slate for near market), or "~13% above market · $529k median · 201 comps" (amber for above market). Self-suppresses when fewer than 4 same-family comps exist (honesty floor). The full EstimatePanel below the Price card is unchanged — the inline hint is additive, giving buyers the key market signal at the price before they read the full analysis. _(cycle: aircraft-price-inline-market)_
- **Aircraft sellers can now add an optional phone number to their listing, and buyers see a "Or call / text: (555) 000-0000" link below the Message button on the listing detail page.** The "Phone (optional)" tel input was added to the "More details" section of `/aircraft/new`, with helper text "Shown to buyers on your listing if you'd like them to call or text." On the detail page, the phone number renders as a `tel:` link only for user-posted listings that have a phone set — self-suppresses for scraped listings and for user-posted listings without one. Closes the parity gap vs. the partnership post form which has always collected contact_phone. No schema change: the `contact_phone text` column and `createAircraftListing` action already accepted the field; this cycle surfaces it in the UI. _(cycle: aircraft-contact-phone)_
- **Landed the partnership IFR suitability badge** (built and fully QA'd in the prior cycle but blocked by a stale `.next` build cache causing phantom-chunk 404s). The fix was clearing `.next` and rebuilding clean — the smoke gate passed immediately (6/6: HTTP 200, zero console errors, zero overflow). The feature: partnership detail pages now show a coloured IFR suitability verdict badge at the top of the Avionics & Panel section ("Glass panel — IFR touring setup", "IFR-capable: WAAS GPS + autopilot", etc.) so co-ownership buyers get the same synthesized IFR read that aircraft-for-sale listing pages already show. The shared `computeIfrSuitability` function was extracted from the aircraft page into `src/lib/avionicsClassify.ts` (12 unit tests) so both pages import one copy — aircraft behaviour unchanged. The prior FAIL's root cause was environmental: stale `.next` artifacts from earlier in the session caused the build to emit HTML referencing chunk filenames that weren't written to disk; a clean `rm -rf .next && npx next build` resolved it. _(cycle: avionics-ifr-land)_
- ⚠️ FAILED — Ported the honesty-gated **IFR-suitability buyer read** (shipped to aircraft listings in `avionics-ifr-summary`) onto the **partnership detail page's** Avionics panel, so co-ownership buyers get the same "can I file IFR in this shared aircraft, and how capable is the panel?" synthesis instead of a bare capability-chip list. Refactor: extracted `computeIfrSuitability` + `IfrTier`/`IfrSuitability` out of the aircraft page into the shared `src/lib/avionicsClassify.ts` (added `avionicsClassify.test.ts`, 12 unit tests) so both detail pages import one copy; aircraft behaviour unchanged. The code is complete and correct — **`npx next build` + typecheck pass, all 12 unit tests pass, and the rendered screenshots show the partnership avionics panel with the IFR badge + sub-line, zero horizontal overflow at 1280 + 375, HTTP 200.** _(cycle: partnership-avionics-ifr)_
- **Aircraft listing pages now synthesize the extracted avionics list into a plain-English IFR suitability verdict at the top of the Avionics & Panel section.** Before, the panel showed capability chips (Glass panel, ADS-B Out, Autopilot, WAAS GPS) and a raw equipment list, but gave no synthesized read on what those chips mean for IFR capability. Now a colored verdict badge appears above the chips — "Full IFR touring setup" (emerald, when glass + WAAS + autopilot are all present), "IFR-capable: WAAS GPS + autopilot" (sky blue), "Glass panel with autopilot" (sky blue), and so on down to "ADS-B compliant" (slate) for aircraft with compliance hardware but no nav suite. An explanatory sentence follows the badge. For full/capable IFR tiers, the same verdict also appears as a positive row in the "How this stacks up" Deal Score tally — so a buyer sees the IFR capability signal in the main verdict even before scrolling to the Avionics section. Self-suppresses when no capability chips were detected (raw equipment list only, no recognizable glass/GPS/autopilot patterns) — no fabricated IFR claim. _(cycle: avionics-ifr-summary)_
- **The ClubHanger Estimate sidebar panel on aircraft listing detail pages now also shows how long the listing has been on the market, with a relative comparison to similar active aircraft.** Before, "Listed N days ago" appeared only in the main-column header and in the Deal Score tally below the fold — the sidebar, where buyers look when evaluating price, had no listing-age context. Now the EstimatePanel footer shows: "Listed 14 days ago — on the market longer than ~65% of the 66 similar Cessna 172 listings for sale now — seller may have flexibility." When the listing is freshly listed, it reads "Listed 3 days ago — on the market about as long as the typical Cessna 172 listing." When it's been sitting longer but still < 30 days, the seller-flexibility inference is omitted (honesty: flexibility language only appears when the listing is both absolutely stale ≥ 30 days AND relatively older than its comp set). Self-suppresses when `first_seen_at` is null or the comp set is too thin. _(cycle: listing-age-context)_
- **The aircraft listing's "How this stacks up" verdict now also reflects how much engine life is left before an overhaul — not just price, listing age, annual status, damage, and spec completeness.** Before, the engine's hours-since-overhaul vs. its published TBO lived only in the Engine Life panel further down the page; now it also rolls up into the at-a-glance tally at the top. A buyer scanning the summary immediately sees, e.g., "Engine has life left — 1,680 hrs to TBO (84% of the 2,000-hr Continental IO-550 interval remaining)" as a point in the listing's favor, "Mid-time engine" as a neutral note, or "Engine past TBO" / "Approaching TBO" as something to ask about — and the "N in this listing's favor / N to ask about" chips count it automatically. _(cycle: deal-score-engine-life)_
- **The aircraft listing's "How this stacks up" verdict now also reflects the plane's maintenance situation — its annual-inspection status and whether prior damage is reported — not just price, listing age, and spec completeness.** Before, those two reads lived only in their own panels further down the page; now they also roll up into the at-a-glance tally at the top, and the "N in this listing's favor / N to ask about" chips count them automatically. So a buyer scanning the summary immediately sees, e.g., "Annual may be overdue — confirm a current annual" or "No damage reported" alongside the price-vs-comps verdict. _(cycle: deal-score-maintenance-signals)_

---

## /partnerships/seeking/new  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/partnerships/seeking/new)

- **The "Also flying from" airport field on the Seeker post form is now pre-filled by the AI draft when a pilot mentions a second airport in their notes.** Before, pasting notes like "I'm based at KPAO but regularly fly out of KSQL too" would fill `home_airport=KPAO` correctly but leave the "Also flying from" field empty, requiring manual entry. Now `generateSeekerDraft` extracts `additional_airport_2` (the second ICAO code) via an added tool-schema property, and `PostSeekerListingForm` fills `[name="additional_airport_2"]` from the result. The seeker form's AI prefill now covers all fields it can extract: home_airport, additional_airport_2, preferred_makes, preferred_models, aircraft_category, year range, max_buy_in/monthly/hourly, willing_to_travel_nm, total_hours, ratings_held, hours_per_month, title, and description — completing the "Paste & prefill the whole form" P1 goal for the seeker form. No schema changes, no UI changes, additive only. _(cycle: seeker-ai-additional-airport)_
- **The email input on the Partnership and Seeker post forms now hides when "Message through ClubHanger" is selected (the default), and reappears when "Email only" or "Email or phone" is chosen.** Before, the email field was always visible in the "More details" section even when the poster kept the default platform-messaging contact method — where the email address is never shown publicly and is therefore irrelevant. Now the field only appears when it's actually needed (email contact selected). The field stays in the DOM when hidden so its value is preserved if the poster changes their mind. Switching back to platform messaging hides it again. Draft restoration also correctly syncs the hidden/visible state. _(cycle: platform-contact-email-hide)_
- **Posting a partnership or seeking listing now defaults to on-platform messaging instead of exposing the poster's email.** Both post forms gain "Message through ClubHanger (default)" as the first "Preferred Contact Method" option, ahead of the existing "Email only / Phone only / Email or phone" choices, and the server actions (`createPartnership`, `createSeekerListing`) now fall back to `contact_method='platform'` instead of `'email'` when nothing is submitted. The misleading "Not shown publicly" helper text under the email field — which was wrong for the `email`/`both` cases — is corrected to "Only shared when you select email contact above." Net effect: a new poster who doesn't actively choose email keeps their address private and routes buyers through the in-app Message thread, which both protects privacy and keeps engagement on-site. Existing listings are untouched (rows with `contact_method='email'` still render "Send Email"); `ContactButtons`/`SeekerContactBar` already suppress email/phone links and show only the Message button for `platform`, so no component change was needed. _(cycle: post-platform-default)_
- **Pilots seeking a partnership can now specify a second home airport on the "Post a Seeking Listing" form.** Before, a seeker could only enter one ICAO — a real limitation for pilots who live near two airports (e.g., KPAO and KNUQ, or SFO-area pilots equidistant between several fields). Now "The basics" section shows an optional "Also flying from" airport input directly below the required home airport, with helper text "Based near two airports? Add the second so owners at either can find you." On the seeker's detail page, any additional airports appear alongside the primary — e.g., "KPAO · Palo Alto, CA · also: KNUQ." The feature requires a human-applied DB migration (`alter table partnership_seekers add column if not exists additional_airports text[];`) — until applied, the form still submits correctly and the listing goes live without the additional airport (graceful self-suppression in the action, same pattern as `partnership-post-spec-fields`). _(cycle: seeker-additional-airports)_
- **The "looking for a partnership" post form now has one-tap chips for the most common aircraft models above the "Preferred Models" box.** Before, a pilot had to recall and type each model as comma-separated text (e.g. "SR22, 182"); now they can just tap 172 / 182 / SR22 / SR20 / Cherokee / Arrow / M20 / Bonanza / DA40 to add it, tap again to remove it, and still type any other model by hand. Active chips light up in sky-blue, and anything typed manually (or filled by the AI "Prefill from your notes" button) keeps the chips in sync. The field stays free text — no change to the data model, server action, or validation. _(cycle: seeker-model-chips)_
- **The "looking for a partnership" post form now has one-tap chips for common pilot ratings and endorsements above the "Ratings & Endorsements" text box.** Before, a pilot had to recall and type each rating as comma-separated abbreviations (e.g. "PPL, IFR, Complex"); now they can just tap PPL / IFR / Complex / High Performance / Multi-Engine / Tailwheel / CFI / ATP to add it, tap again to remove it, and still type any unlisted rating by hand. Active chips light up in sky-blue so it's clear what's selected, and anything typed manually (or filled by the AI "Prefill from your notes" button) keeps the chips in sync. The field stays free text — no change to the data model, server action, or validation. _(cycle: seeker-ratings-chips)_
- **The "looking for a partnership" post form now has one-tap make chips above the "Preferred Makes" box.** Before, a pilot had to remember and type each make as comma-separated text (e.g. "Cessna, Piper, Cirrus"); now they can just tap Cessna / Piper / Beechcraft / Cirrus / Mooney / Van's / Diamond / Grumman to add it, tap again to remove it, and still type any other make by hand. Tapped makes light up so it's clear what's selected, and anything typed by hand (or filled by the AI "Prefill from your notes" button) keeps the chips in sync. _(cycle: seeker-make-chips)_

---

## /partnerships/new  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/partnerships/new)

- **The description textarea on the Partnership post form is now visible above the fold without needing to expand "More details."** Before, description lived inside the "More details" collapsible alongside Year, TTAF, SMOH, Engine, and Title — posters who never tapped it published thin listings with no text. Now a new "About this partnership" section sits between Photos and "More details," always visible, with a compelling prompt ("Tell prospective partners about the aircraft, the current group, how scheduling works, and what you're looking for in a partner. A compelling description is the single biggest factor in getting a serious inquiry."). Title stays inside "More details" (still auto-filled from make/model). AI prefill still fills `[name="description"]` correctly (same selector, different DOM position). The `hasOptional` auto-open check was updated to no longer trigger on `description` (now outside `<details>`). Mirrors the `aircraft-description-above-fold` fix from an earlier cycle. _(cycle: partnership-description-above-fold)_
- **The email input on the Partnership and Seeker post forms now hides when "Message through ClubHanger" is selected (the default), and reappears when "Email only" or "Email or phone" is chosen.** Before, the email field was always visible in the "More details" section even when the poster kept the default platform-messaging contact method — where the email address is never shown publicly and is therefore irrelevant. Now the field only appears when it's actually needed (email contact selected). The field stays in the DOM when hidden so its value is preserved if the poster changes their mind. Switching back to platform messaging hides it again. Draft restoration also correctly syncs the hidden/visible state. _(cycle: platform-contact-email-hide)_
- **Posting a partnership or seeking listing now defaults to on-platform messaging instead of exposing the poster's email.** Both post forms gain "Message through ClubHanger (default)" as the first "Preferred Contact Method" option, ahead of the existing "Email only / Phone only / Email or phone" choices, and the server actions (`createPartnership`, `createSeekerListing`) now fall back to `contact_method='platform'` instead of `'email'` when nothing is submitted. The misleading "Not shown publicly" helper text under the email field — which was wrong for the `email`/`both` cases — is corrected to "Only shared when you select email contact above." Net effect: a new poster who doesn't actively choose email keeps their address private and routes buyers through the in-app Message thread, which both protects privacy and keeps engagement on-site. Existing listings are untouched (rows with `contact_method='email'` still render "Send Email"); `ContactButtons`/`SeekerContactBar` already suppress email/phone links and show only the Message button for `platform`, so no component change was needed. _(cycle: post-platform-default)_
- **The "Post a Partnership" form's Buy-In Price field is now optional.** Before, every owner posting a partnership had to enter a specific buy-in amount — a real blocker for owners who haven't set a price yet or want to list as "price negotiable / discuss with buyer." Now the field shows "(optional)" in its label and helper text reads "Leave blank if price is negotiable — describe the terms in your listing." The Info tooltip also mentions the negotiable path. The server action and DB already accepted null buy_in_price (integer, no NOT NULL constraint); this just removes the client-side `required` gate. Analysis panels (PartnerMarketCheck, PartnerShareCostPanel) already self-suppress when buy_in is null — no regression. _(cycle: partnership-buyin-optional)_
- **The "Post a Partnership" form's "More details" section now lets aircraft owners enter their plane's Total Time (TTAF), time since overhaul (SMOH), and engine designation alongside the existing Year field.** Before, the partnership post form had no way to capture these spec fields — so every user-posted partnership listing would silently suppress the Engine Life, Airframe Time, and Overhaul Timeline analysis panels that buyers see on the detail page (those panels only fire when the data exists). Now an owner can type "Lycoming IO-360" and "600" SMOH and those values flow to the listing, lighting up all three analysis panels automatically. The fields are optional, live in the collapsed "More details" block so they don't add friction to the fast path, and match the exact same inputs, labels, and helper text on the aircraft post form. The AI "Prefill from your notes ✨" button also now extracts TTAF/SMOH/engine from pasted listing blobs and fills these fields. _(cycle: partnership-post-spec-fields)_
- **The Make field on the "Post a Partnership" form is now a type-anything box with suggestions, instead of a fixed dropdown that forced every uncommon make into "Other."** Before, a partner posting a Maule, Husky, Bellanca, Aviat (etc.) had to pick "Other" — which dropped their real make, so their listing never matched our make/model comp set or the per-make family pages. Now you type the make (with a suggestion list of common ones), and the FAA tail-number lookup and the AI "Prefill from your notes" still fill it in automatically. This brings the partnership form to parity with the for-sale aircraft form, which got this same change last week. _(cycle: partnership-make-freetext-datalist)_

---

## /aircraft — Planes for Sale (marketplace)  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/aircraft)

- **Aircraft browse cards now show the market median price inside the comp pill.** Before, a comp pill like "~47% below avg · 37 comps" told buyers the direction and depth of market coverage, but gave no absolute dollar anchor — is the median $100k or $400k? Now it reads "~47% below avg · $135k · 37 comps", so a buyer scanning the grid can immediately interpret the percentage as a real dollar gap. The tooltip on hover also shows the full median ("vs. median $134,900"). A new `formatPriceK()` compact formatter was added to utils.ts to produce "$Xk" / "$X.XM" labels that fit inside badges without clipping. _(cycle: comp-pill-median-price)_
- **Aircraft browse cards now show the number of comparable listings behind the "X% below/above average" claim.** Before, a comp pill like "~18% below average" gave buyers no sense of whether that comparison was backed by 4 listings or 40. Now it reads "~18% below avg · 15 comps" (or "Near avg · 15 comps", "~X% above avg · N comps"), so buyers scanning the grid immediately see the market depth behind the verdict. The count is always ≥ 4 (the existing MIN_OTHER_COMPS honesty floor). No change to the DealCheckChip path (comp is still null when dealVerdict is present). `CompResult` now carries `count` and `median` fields, which future slices can use (e.g., showing the reference median price on hover or in a tooltip). _(cycle: browse-comp-count)_
- **Aircraft browse cards now show a synthesized IFR verdict badge for glass-panel and capable-IFR aircraft instead of raw individual avionics chips.** Before, a Cirrus SR22 with G1000+WAAS+autopilot showed "Glass panel" + "ADS-B Out" as two separate chips — useful, but not a buyer answer. Now it shows "Full IFR setup" (emerald) or "IFR-capable: WAAS GPS + autopilot" (sky-blue) as a single synthesized verdict chip, so a buyer scanning the browse list gets an immediate IFR suitability read without clicking into the detail page. Aircraft without IFR-grade avionics keep their individual capability chips unchanged. Four IFR verdict badges were confirmed on the live browse page; 20 individual avionics chips remain on non-qualifying aircraft. _(cycle: ifr-badge-browse-cards)_

---

## /partnerships/seeking/[id]

- **Posting a partnership or seeking listing now defaults to on-platform messaging instead of exposing the poster's email.** Both post forms gain "Message through ClubHanger (default)" as the first "Preferred Contact Method" option, ahead of the existing "Email only / Phone only / Email or phone" choices, and the server actions (`createPartnership`, `createSeekerListing`) now fall back to `contact_method='platform'` instead of `'email'` when nothing is submitted. The misleading "Not shown publicly" helper text under the email field — which was wrong for the `email`/`both` cases — is corrected to "Only shared when you select email contact above." Net effect: a new poster who doesn't actively choose email keeps their address private and routes buyers through the in-app Message thread, which both protects privacy and keeps engagement on-site. Existing listings are untouched (rows with `contact_method='email'` still render "Send Email"); `ContactButtons`/`SeekerContactBar` already suppress email/phone links and show only the Message button for `platform`, so no component change was needed. _(cycle: post-platform-default)_
- **Seeking listings now appear on the "My Listings" management page, and the partnership and seeker post-success banners now include a "View all my listings →" link.** Before, pilots who posted a "seeking partnership" listing had no way to find or manage it on `/listings` — the page only showed aircraft and partnerships. After posting a seeker listing, a pilot visiting `/listings` now sees a "Pilots seeking (N)" section with status badge, title, home airport, and a "View →" link to their listing. Separately, the post-success banners on partnership and seeker detail pages (shown with `?posted=1`) now include a "View all my listings →" link to `/listings`, matching what the aircraft success banner already showed. _(cycle: seeker-on-listings-page)_
- **Pilots seeking a partnership can now specify a second home airport on the "Post a Seeking Listing" form.** Before, a seeker could only enter one ICAO — a real limitation for pilots who live near two airports (e.g., KPAO and KNUQ, or SFO-area pilots equidistant between several fields). Now "The basics" section shows an optional "Also flying from" airport input directly below the required home airport, with helper text "Based near two airports? Add the second so owners at either can find you." On the seeker's detail page, any additional airports appear alongside the primary — e.g., "KPAO · Palo Alto, CA · also: KNUQ." The feature requires a human-applied DB migration (`alter table partnership_seekers add column if not exists additional_airports text[];`) — until applied, the form still submits correctly and the listing goes live without the additional airport (graceful self-suppression in the action, same pattern as `partnership-post-spec-fields`). _(cycle: seeker-additional-airports)_

---

## /aircraft/[make] — Make-level for-sale pages  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/aircraft/new)

- **The description field on the aircraft post form is now visible above the fold without needing to open "More details."** Before, title and description lived inside the "More details (optional)" collapsible alongside Year, TTAF, SMOH, and Engine — sellers who never tapped "More details" submitted listings with no description, producing thin, unconvincing listings. Now a new "About this aircraft" section sits between Photos and "More details," always visible, with a helpful prompt ("Tell buyers what makes this aircraft special — avionics, engine times, recent work, paint/interior, and why you're selling"). Year, TTAF, SMOH, Engine, Title, and Phone remain collapsible in "More details." AI prefill still fills the description field correctly (same `[name="description"]` selector; DOM position doesn't affect the fill). The `hasOptional` auto-open logic was updated to no longer trigger on `description` (now outside `<details>`). _(cycle: aircraft-description-above-fold)_
- **Aircraft sellers can now add an optional phone number to their listing, and buyers see a "Or call / text: (555) 000-0000" link below the Message button on the listing detail page.** The "Phone (optional)" tel input was added to the "More details" section of `/aircraft/new`, with helper text "Shown to buyers on your listing if you'd like them to call or text." On the detail page, the phone number renders as a `tel:` link only for user-posted listings that have a phone set — self-suppresses for scraped listings and for user-posted listings without one. Closes the parity gap vs. the partnership post form which has always collected contact_phone. No schema change: the `contact_phone text` column and `createAircraftListing` action already accepted the field; this cycle surfaces it in the UI. _(cycle: aircraft-contact-phone)_

---

## (site-wide)

- **Sellers can now see their closed and sold listings on the /listings management page, and relist them in one click.** Before, once a seller marked their aircraft as sold, closed their partnership, or deactivated a seeking listing, those listings disappeared from /listings with no way to recover them without re-posting from scratch. Now a collapsible "Past listings (N) — sold & closed" section appears at the bottom of /listings showing all three types. Each past listing shows a "Sold" or "Closed" status badge, the title, key details, a "View →" link, and a "Relist" button. Clicking "Relist" shows a confirm dialog, then flips the listing's status back to active (and resets `posted_at` to now for partnerships so they re-appear at the top of date-sorted browse feeds). The listing moves from Past listings back to the active section on the same page. _(cycle: past-listings-relist)_
- **Listing owners can now close or deactivate their own listings directly from the /listings management page.** Before, the page only had a "View →" link — a seller whose plane sold or a partnership owner whose spots filled had no way to remove their listing from ClubHanger without contacting support. Now each aircraft row has a "Mark as sold" button, each partnership row has a "Close listing" button, and each seeker row has a "Deactivate" button. Each uses a `window.confirm` dialog to prevent accidental clicks, shows a "Closing…" pending state, and disappears the listing from /listings once the action completes. The server action (`deactivateListing`) is owner-gated: it verifies `poster_id = user.id` in the DB query (defense-in-depth on top of the existing RLS `update using (auth.uid() = poster_id)` policies that already protect all three tables). _(cycle: listings-deactivate-action)_

---

## /partnerships — Browse partnerships  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/partnerships)

- **Partnership browse cards now show the market median buy-in and comp count inside the "below/above market" chip.** Before, a card with a comp verdict read `"~28% below market"` — a buyer knew the direction but had no dollar anchor. Now it reads `"~28% below market · $25k · 4 comps"`, so a buyer scanning the partnership list immediately sees both the depth of the gap and how many same-make partnerships the comparison is drawn from. A hover tooltip shows the full figure: `"vs. median buy-in $25,000"`. This exactly parallels what `comp-pill-median-price` added to aircraft browse cards last cycle. The `PartnerCompResult` from `partnershipComps.ts` already returned `median` and `count`; this cycle wires them through `PartnershipList → PartnershipCard` — no new DB queries, no schema changes, additive only. _(cycle: partnership-comp-pill-enriched)_

---

## /partnerships/seeking — Pilots seeking shares  ·  [open ↗](https://aeromatch-git-staging-zealoustiger-7853s-projects.vercel.app/partnerships/seeking)

- **When filtering the "pilots seeking a partnership" list by airport, owners now also see seekers who listed that airport as their second home airport.** Before, the `getSeekers()` query only matched on `home_airport` — a pilot who posted KPAO as primary and KNUQ as their "Also flying from" airport was invisible to owners searching at KNUQ. Now the query uses a PostgREST OR-filter (`home_airport.in.(…) OR additional_airports.ov.{…}`) so both airports count equally for discoverability. Graceful fallback to home_airport-only if the seeker_additional_airports DB migration hasn't been applied yet (same pattern as createSeekerListing). Mock-data path updated in parallel. _(cycle: seeker-airport-or-filter)_

---

## ⚠️ Needs your attention
- **partnership-avionics-ifr** failed QA — left as a branch for review. Ported the honesty-gated **IFR-suitability buyer read** (shipped to aircraft listings in `avionics-ifr-summary`) onto the **partnership detail page's** Avionics panel, so co-ownership buyers get the same "can I file IFR in this shared aircraft, and how capable is the panel?" synthesis instead of a bare capability-chip list. Refactor: extracted `computeIfrSuitability` + `IfrTier`/`IfrSuitability` out of the aircraft page into the shared `src/lib/avionicsClassify.ts` (added `avionicsClassify.test.ts`, 12 unit tests) so both detail pages import one copy; aircraft behaviour unchanged. The code is complete and correct — **`npx next build` + typecheck pass, all 12 unit tests pass, and the rendered screenshots show the partnership avionics panel with the IFR badge + sub-line, zero horizontal overflow at 1280 + 375, HTTP 200.**

---

## 🧪 Code-quality spot-checks — 10 judged, avg 4.4/5

- **partnership-comp-pill-enriched — 4/5** — none material — the below/above chip markup is duplicated rather than factored, but that duplication pre-existed this cycle and matches the file convention.
- **ifr-badge-browse-cards — 4/5** — `computeIfrSuitability(caps)` runs twice per card (once for `ifrTier` in the body, again inside `IfrCardBadge`) instead of computing once and passing the result down; the `IFR_CARD_CHIP` map carries unreachable `equipped`/`basic` keys (badge returns null for them); and `title={ifr.sub}` adds hover text that the spec listed as explicitly out of scope. All immaterial.
- **avionics-ifr-land — 4/5** — The 4-line `IFR_CHIP` color map is now copy-pasted into both detail pages — a deliberate mirror of the existing per-page `CAP_COLORS` pattern, so defensible, but it's a second presentation site that can drift from the shared tier enum. Nothing material.
- **seeker-on-listings-page — 4/5** — Minor — a local `SeekerRow` type is declared rather than reusing/`Pick`-ing the existing `PartnershipSeeker` interface in `lib/types`, though that interface lacks `title`/`status`/`poster_id` so a narrow local type is defensible; the internal `/partnerships/seeking/[id]` "View" link uses the `ExternalLink` icon, a slight semantic quibble. Nothing material.
- **seeker-airport-or-filter — 4/5** — Fallback path re-builds the makes/ratings/share_type/min_hours chain verbatim (~7 duplicated lines) instead of a shared helper — two sites that can drift; also airport codes are interpolated into the `.or()` string unquoted (safe for A-Z0-9 ICAOs, but no sanitization).
- **listing-age-context — 4/5** — The relative-recency phrasing is hand-rolled a second time as an inline JSX ternary, duplicating `domDetail`'s longer/shorter/typical branching with slightly reworded copy ("seller may have flexibility" vs "a seller-flexibility signal"; "similar … for sale now" vs "comparable … still for sale") — two render sites that can drift, and AC#2's "matching the language already used in the tally" is only loosely honored.
- **partner-buyin-inline-market — 5/5** — none material — slight semantic redundancy with the full market panel below, but that proximity-at-the-price-moment is precisely the spec's stated intent; the colored label is also backed by literal "below/above/Around market" text, so it's not color-only for a11y.
- **platform-contact-email-hide — 5/5** — none material — select stays uncontrolled (relies on first option being the default), but that matches the file's established convention.
- **partnership-buyin-optional — 5/5** — none material — helper text + updated tooltip both mention the negotiable path (minor copy overlap, intentional progressive-disclosure).
- **partnership-implied-value — 5/5** — none material — detail copy says "comparable {make} aircraft" (make-only) though comps are model-family-filtered, so the label slightly understates specificity; cosmetic, not misleading.

---

## To ship
Tell Claude "promote /aircraft" (or any pages above), or "promote everything." Claude merges the chosen work staging→main, which deploys to clubhanger.com.
